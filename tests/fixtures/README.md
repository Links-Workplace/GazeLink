# Test fixtures

Fixtures must be synthetic, generated, or explicitly approved for local testing.

Do not commit raw camera frames, face images, screenshots, biometric recordings, or user-identifiable calibration data. Hardware-dependent fixtures and tests must be separated from the default automated suite.

