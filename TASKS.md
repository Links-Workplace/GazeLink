GAZELINK — תוכנית עבודה ברורה

גרסת הסבר ועבודה, 6 בספטמבר 2026.

מבוססת על מסמך המשימות שצורף ועל ההחלטות בשיחה. הקוד והמצלמה לא נבדקו במסגרת השכתוב. סטטוס ביצוע להלן הוא דיווח מהמסמך המקורי, ולא אימות חדש. מסמך המקור נשאר ההיסטוריה המפורטת של הניסויים, השינויים והמדידות.

1. מה אנחנו בונים

מערכת Python על Windows שמקבלת תמונה ממצלמת רשת רגילה, מזהה לאן המשתמש מסתכל על המסך ובהמשך מאפשרת להפעיל מחשב באמצעות מבט, קריצה או שהייה על כפתור.

המצלמה הנוכחית ממוקמת מתחת למסך. המטרה היא לבדוק את המערכת בתנאים האלה. הדגם הרשמי של מצלמת הייחוס עדיין טעון אישור לפי המסמך.

2. איפה אנחנו נמצאים עכשיו

תחום

מה קיים לפי המסמך

מה עדיין חסר

תשתית הפרויקט

מבנה קוד, הגדרות, לוגים, בדיקות ובנייה מקומית

אימות סביבת התקנה חדשה והרצה מרוחקת של הבדיקות

מצלמה וזיהוי עיניים

מצלמה עובדת, זיהוי פנים, עיניים, קשתית ופתיחת עפעפיים

בדיקה ממושכת והשלמת בדיקות על המסך האמיתי

המנוע העצמאי

איסוף כיול, מודלים לחיזוי נקודה, תיקון מקומי ומסננים

דיוק חי עקבי לא הוכח; הפיתוח הזה מושהה כרגע

EyeGestures

מתאם קיים; מסלול מדידה מלא נכתב ועבר בדיקה אוטומטית — כיול 36 נקודות, עצירת למידה מאומתת, כתיבת תוצאות

אימות מול מצלמה: דיוק, זמינות נתונים וביצועים. טרם בוצע

כלי בדיקה

מסך מטרות, הצגת תחזיות, כלי ניתוח וייצוא; הותאמו למסלול EyeGestures ומודדים את מה שמוצג — עבר בדיקה אוטומטית

הרצה מול מצלמה שתייצר את המספר הראשון

שליטה במחשב

תוכננה

תנועת עכבר, לחיצות ומחוות עדיין מסומנות כלא התחילו

מוצר מלא

תוכנן

ממשק נגיש, מקלדת, פרופילים, התקנה ופיילוט

המשמעות: קיימת תשתית משמעותית. עדיין אין במסמך הוכחה שהמערכת מספקת שליטה מדויקת ואמינה במחשב.

סיום הכיול אומר שתהליך איסוף הנתונים הסתיים. הוא אינו מוכיח שהתחזיות מדויקות. מעבר בדיקות קוד מוכיח את ההתנהגות שנבדקה בהן, ואינו מחליף בדיקה עם משתמש ומצלמה.

3. ההחלטה הנוכחית

מתמקדים ב־EyeGestures כמנוע חיזוי המבט הפעיל בניסוי הנוכחי.

משאירים את המנוע העצמאי בקוד, בלי להמשיך לפתח אותו כרגע ובלי להריץ אותו במקביל לצורך השוואה.

משאירים את שילוב Tobii Nexus בצד. אין צורך למחוק אותו.

Beam נשאר אפשרות שנייה אם הבדיקה הנקייה של EyeGestures לא תעמוד בדרישות. אין להטמיע אותו עכשיו.

ממשיכים ב־Python ובפרויקט הקיים.

משתמשים במסך gaze-test הקיים ככל שניתן, לאחר התאמתו למסלול EyeGestures.

במסלול הבדיקה של EyeGestures מנתקים את תיקון תנוחת הראש הנוסף שלנו. שומרים רק התאמות קלט ותצוגה שנדרשות ומוסברות.

שליטה בעכבר ולחיצות נשארות מושבתות במהלך בדיקות הדיוק.

החלטות אלה הן כיוון העבודה מהשיחה. אין להסיק מכך שהשינויים כבר בוצעו בקוד.

4. מי אחראי על מה

רכיב

אחריות

EyeGestures

עיבוד תמונת המצלמה והפקת תחזית מבט לפי הגרסה והכיול שלה

המתאם שלנו

העברת תמונה בפורמט הנכון, ניהול מצב הספרייה והמרת הפלט לחוזה של האפליקציה

תהליך הכיול שלנו

הצגת מטרות לפי דרישות הספרייה, תזמון ומעקב אחר התקדמות וסיום

שכבת המסך שלנו

התאמה בין תחזית המבט לבין המסך והחלון, כולל גודל, מיקום וקנה מידה

מסך הבדיקה שלנו

הצגת מטרה ותחזית, שמירת הנתונים וחישוב השגיאה

עיבוד נוסף שלנו

החלקה ותיקון, רק בשלב נפרד ואחרי מדידת פלט הבסיס

שליטה במחשב

בהמשך: מחוות, החלטה מתי ללחוץ, הזזת סמן, עצירה והתאוששות

אין להעביר את תחזית EyeGestures דרך מודל הכיול של המנוע העצמאי בלי צורך מפורש שנבדק. מנוע חיזוי יחיד אינו מחייב למחוק את כל רכיבי זיהוי הפנים הקיימים; צריך לבדוק מה תפקידם ואם הם חוסמים נתונים או מוסיפים עיבוד מיותר.

5. העבודה הקרובה — לפי הסדר

A. למפות את המסלול הפעיל

מצב: הושלם. אומת בקריאת קוד, כולל קוד הספרייה המותקנת.

ראיות: EyeGestures 3.2.4, המחלקה EyeGestures_v2. הספרייה מקבלת פיקסלים של המסך ומחזירה פיקסלים. החשבון היחיד שהקוד שלנו עושה על נקודת המבט הוא חלוקה לנרמול. המצלמה בבעלות יחידה של VisionRuntime; הספרייה לא נוגעת בה.

שלושה ממצאים ששינו את התוכנית:

שער הביטחון שלנו חסם את הפלט של הספרייה, לא את הזנתה. שתי הצהרות בתיעוד טענו את ההפך ותוקנו.

calibration_radius אינו מנוף לעצירת הלמידה: תנאי המרחק ממוזג ב-or עם תנאי שתמיד מתקיים, ולכן הספרייה מתאמנת בכל פריים ללא קשר למרחק. המנוף היחיד הוא הארגומנט calibration ל-step.

הספרייה מחליקה בעצמה — חוצץ של 20 פריימים מעורבב עם מנוע שני. כלומר ההשהיה שלה כ-1.3 שניות, וחלון ההתייצבות הקודם של 750 מילישניות היה קצר ממנה.

לזהות את גרסת EyeGestures ואת המימוש שבו משתמשים בפועל.

לתעד מהמצלמה עד הנקודה המוצגת: מי מקבל תמונה, מי מסנן אותה, מי מחשב מבט ומי מחשב שגיאה.

לוודא שרק תהליך אחד פותח את המצלמה לצורך הניסוי.

לבדוק אם זיהוי הפנים שלנו מונע מ־EyeGestures לקבל תמונות או מונע הצגת תחזיות תקינות.

להפריד בין מגבלה שנמדדה לבין השערה שעדיין לא נבדקה.

תנאי סיום: ברור איזו ספרייה מפיקה את הנקודה ואילו שינויים עוברים הנתונים בדרך למסך.

B. לייצב את הכיול של EyeGestures

מצב: הקוד נכתב ועבר בדיקה אוטומטית. טרם נבדק מול מצלמה.

ראיות: אי-ההתאמה שהמלצת עליה תוקנה — מספר נקודות הכיול נגזר עכשיו מהרשת עצמה ולא נכתב בנפרד, ולכן הוא 36 ואינו יכול להיפרד ממנה. הקודם, 26, השאיר עשר נקודות שהועלו ומעולם לא נוסו, ושום דבר בקוד לא אמר זאת.

שלושת המצבים מופרדים על המסך ובקובץ התוצאות: ENGINE_CALIBRATING, FREEZING, MEASURING. כל דגימה נושאת את השלב שבו נרשמה, כך ששורה מזוהמת ניתנת לזיהוי ולא ממוצעת בשקט.

להפעיל את הכיול של הספרייה בהתאם לממשק שלה, בלי הכיול הנוסף של המנוע העצמאי.

לוודא שהתמונה מועברת בכיוון ובפורמט הצבע הנדרשים.

לוודא שהמטרה שמוצגת תואמת למטרה שהספרייה משתמשת בה באותו זמן.

להפריד בבירור בין ״כיול בתהליך״, ״הכיול הסתיים״ ו״בדיקת דיוק״.

לא להסיק שסיום רצף חזותי מוכיח שהמודל מוכן בלי לבדוק את משמעות הסיום במימוש הספרייה.

המלצה בעקבות צילום המסך האחרון, טרם אושרה כבוצעה: אם מוגדרת רשת של 36 נקודות, להשלים את כל 36 הנקודות ולוודא שנקלטו בפועל. לתקן את אי־ההתאמה שבה המתאם מכריז על סיום אחרי 26. מספר נקודות גדול יותר כשלעצמו אינו הבטחת דיוק.

תנאי סיום: ניתן להשלים כיול עם תיעוד של המטרות שנקלטו ושל תנאי הסיום.

C. להתאים את מסך הבדיקה והמספרים

מצב: הושלם ועבר בדיקה אוטומטית, כולל על ווידג'ט Qt אמיתי.

ראיות: נמצאו שתי המרות שונות — הציור השתמש בהיסט לפי גודל הסמל והניקוד בסרגל אחר. הפער אפס במרכז המסך ולכן לא נראה, אך 37 פיקסלים בקצוות על ווידג'ט אמיתי. כל מסלולי הציור עוברים עכשיו דרך המרה אחת, והזהות בין מרכז הסמל המצויר לפיקסל המנוקד נבדקת בסבילות אפס.

נבדק בנקודות ידועות במרכז, בפינות ובאמצעי הקצוות, על שלוש גיאומטריות וחמישה גדלי סמל, בלי מצלמה. יש גם בדיקה שנכשלת אם מישהו יחזיר את הנוסחה הישנה.

תוקן גם סמן שנשאר על המסך אחרי פריים שנפל — תחזית ישנה שנראית חיה. ואם החלון אינו בגודל שהמדידה מנוקדת מולו, המערכת מסרבת לאסוף במקום להזהיר.

היחידות: כל מספר הוא פיקסל לוגי של Qt. המסך פיזית 5120×1440. נבחר לתייג ולא להמיר, כדי לא ליצור יחידה שנייה ולא לבטל את ההיסטוריה הקיימת.

להציג מטרה ידועה ואת תחזית הספרייה לאחר ההמרה הנדרשת למסך.

למדוד את המרחק מאותה תחזית שמוצגת בפועל, באותו זמן ולא מול מטרה שכבר התחלפה.

לבדוק את גבולות המסך, היסט החלון, קנה המידה של Windows, כיווני הצירים והיפוך התמונה.

לבדוק את ההמרה באמצעות נקודות מלאכותיות במרכז ובפינות, בלי תלות בדיוק המצלמה.

בשלב הבסיס לכבות החלקה, תיקון מקומי והצמדה לכפתורים. אם הספרייה עצמה מעבדת את הפלט, לתעד זאת; אין לכנות אותו נתון חיישן גולמי.

למחוק נקודה שאינה עדכנית ולהציג במפורש שאין כרגע תחזית תקפה.

תנאי סיום: מה שרואים על המסך ומה שמופיע בדוח מתייחסים לאותה נקודה ולאותה מטרה.

D. למדוד דיוק על מטרות חדשות

מצב: הקוד נכתב ועבר בדיקה אוטומטית מקצה לקצה. אין עדיין תוצאת דיוק — היא דורשת מצלמה.

ראיות: המטרות נוצרות עכשיו במרחק גם מרשת ה-36 של הספרייה. קודם, על ה-seed הקבוע, ארבע מתוך עשר ישבו בתוך 250 פיקסלים מנקודת כיול של הספרייה; עכשיו אפס. קרבה כזו אינה הוכחה לשינון, אבל היא מונעת מהמדידה לטעון שהיא בודקת הכללה.

עצירת הלמידה נבדקת ולא מונחת: ממתינים שתהליכוני האימון של הספרייה יסתיימו בפועל, ומשווים את מקדמי המודל לפני ואחרי. אם משהו השתנה, או אם אי אפשר לקרוא את הפנימיות, הריצה מסומנת כאבחון בלבד — וכלי הניתוח אוכף זאת: הוא מסמן את השורה בהיסטוריה כ-DIAGNOSTIC ומשמיט את טבלת גדלי הכפתור לגמרי.

ההמתנה אינה חוסמת את החלון: היא מונחית טיימר, מציגה ספירה ומספר תהליכונים, ויש תקרה של 10 שניות. Esc עובד לכל אורכה.

השגיאה נמדדת על התחזית המקורית, לפני הצמדה לגבולות המסך. תחזית מחוץ למסך נשמרת כמות שהיא ונחשבת תקפה.

זמן ההגעה למטרה נמדד בנפרד מהדיוק, בשלושה רדיוסים, מרגע הצגת המטרה וכולל את חלון ההתייצבות. מטרה שלא הושגה נרשמת כחסרה ולא כערך גדול.

מגבלה שהתגלתה: על מסך 1920×1080 המערכת מסרבת לרוץ. רשת של 36 נקודות ברווח אנכי של 216 פיקסלים אינה משאירה מקום למטרה מוחזקת ברדיוס 250. על 4096×1152 ועל 2560×1440 היא עובדת. הקוד מסרב במקום למדוד פחות נקודות בשקט.

חסם: ההרצה מול המצלמה. הכיול הוא 36 נקודות ויימשך כשלוש עד חמש דקות.

להשתמש במטרות שלא שימשו לכיול, במרכז ובאזורים שונים של המסך.

לוודא שגם ביחס למפת הכיול של EyeGestures המטרות אכן חדשות. ההפרדה מרשת הכיול הישנה של המנוע העצמאי אינה מספיקה.

לא להעביר את מיקום מטרת הבדיקה ללמידה או לתיקון בזמן המדידה.

להפריד בין זמן מעבר המבט לבין חלון המדידה לאחר התייצבות; לתעד את שניהם.

להשתמש במספיק דגימות ובכמה ריצות כדי להציג פיזור וחזרתיות, ולא רק מספר מרשים מניסוי יחיד.

לשמור גאומטריית מסך, הגדרות כיול, מזהי מטרות, זמנים, תחזיות ותקפות נתונים. לא לשמור תמונות מצלמה במסגרת דוח הבדיקה.

מה הדוח צריך להציג:

מדד

הסבר פשוט

חציון שגיאה

המרחק שמתחתיו נמצאות מחצית מהשגיאות שנמדדו

P95

המרחק שמתחתיו נמצאות 95% מהשגיאות שנמדדו; אינו השגיאה המרבית

שגיאה לפי אזור

האם המרכז עובד טוב יותר מהצדדים ומהפינות

רעד

כמה התחזית זזה כשהמבט נשאר על אותה מטרה

זמינות תחזית

כמה מזמן הבדיקה התקבלה תחזית תקפה

זמן תגובה

כמה זמן עובר ממעבר למטרה ועד שהתחזית מגיבה, לפי הגדרה מתועדת

כניסה לשטח כפתור

איזה חלק מהתחזיות התקפות נמצא בתוך כפתור בגודל ידוע סביב המטרה

הוחלט ומומש: כפתורים ריבועיים בגודל 100×100, 200×200, 400×400 ו־800×800 פיקסלים. אלו גדלי בדיקה ולא דרישות מוצר מאושרות. פגיעה מוגדרת כתיבה ולא כרדיוס — גם הסטייה האופקית וגם האנכית חייבות להיות בתוך חצי הצלע — כי כפתור הוא מלבן. להציג גם את שיעור הנתונים החסרים בנפרד. כניסת תחזית לשטח כפתור אינה מוכיחה לחיצה מוצלחת או שליטה מעשית.

מדידה במעלות תתווסף רק עם גאומטריה ומרחק ישיבה מתועדים; אין לקבע המרה כללית בין מעלה לפיקסלים.

תנאי סיום: דוח שאפשר לשחזר, עם תוצאות על מטרות חדשות והבחנה בין דיוק, יציבות ונתונים חסרים.

E. לקבל החלטה לפי התוצאה

אם התצוגה והמדידה אינן מתואמות: לתקן את החיבור לפני שיפוט הספרייה.

אם המסלול תקין והביצועים מתאימים למשימות שנבחרו: להתקדם להחלקה מדודה ולשליטה בסיסית.

אם המסלול תקין אך הביצועים אינם מספיקים: לתעד את המגבלה ולבחון את Beam באותו פרוטוקול בדיקה.

לא לשנות ספי הצלחה בדיעבד כדי להפוך תוצאה ל־PASS.

תנאי סיום: החלטה מנומקת על המשך EyeGestures או בדיקת החלופה, עם מדדים ודרישת שימוש ברורה.

6. מפת הפרויקט המלאה

המזהים נשמרו כדי שיהיה אפשר לקשר לאפיון ולהיסטוריה המקוריים. סטטוס המנוע העצמאי מתאר את העבודה שכבר נעשתה; הוא אינו משימה פעילה כרגע.

מזהה

יכולת

מצב לפי התיעוד / מה נותר

F-01

הגדרת מחשב, מסך ומצלמת ייחוס

חלקי; אישור דגם המצלמה חסר

F-02

תשתית קוד ובדיקות

הושלם מקומית; Git/Remote/הרצה בסביבה חדשה עדיין דורשים אימות לפי המסמך

M1-01

פתיחת מצלמה וזיהוי פנים ועיניים

הושלם לפי התיעוד

M1-02

נתוני עיניים ומצב מעקב

הושלם; ספי תנוחת ראש אינם ערכי מוצר סופיים

M1-03

חלון חי ואבחון

חלקי; תצוגה, יציאה והתאוששות במסך אמיתי

M1-04

בדיקות ראייה מסכמות

חלקי; 10 דקות רצופות, CPU וזיכרון, כמה פנים וסגירת עין מכוונת

M2-01

איסוף כיול עצמאי

ברובו קיים; מסלול מושהה, בדיקות ביטול/התחלה מחדש ואובדן מצלמה נותרו

M2-02

שמירת דגימות ובקרת איכות

קיימים מימושים ותיקונים; מסלול מושהה, חלק מהספים והגרסאות לא אומתו במלואם

M2-03A

מודל חיזוי עצמאי

מומש ונבדק, כולל ניסויים חיים עם בעיות דיוק; פיתוח מושהה

M2-03B

תיקון מקומי למודל העצמאי

תשתית קיימת; אימות חי מלא לפני/אחרי לא הושלם, פיתוח מושהה

M2 — EyeGestures

חיבור ספרייה לחיזוי מבט

המסלול הפעיל להמשך; להשלים שלבים A–E למעלה

M2-04

יציבות, מדידה והדגמת מבט

חלקי; להתאים ולמדוד במסגרת המסלול הפעיל

ראיות סקירה, 7.9.2026: הושוותה מערכת Eyecom לקוד הנוכחי; לא נמצא בה מודל כיול מחובר או אימות דיוק שמצדיק החלפת מנוע. 532 בדיקות הפרויקט עברו. אין מדידת חומרה חדשה ואין שינוי בסטטוס; ראו סעיף 12.

פער שנמצא בסקירה: gaze-test מעביר 36 נקודות, אך gaze_window עדיין משתמש בברירת המחדל של 26 שב־eyegestures_engine. טענת השלמת 36 נקודות בסעיפים הקודמים נכונה למסלול המדידה בלבד. נדרש יישור או תיעוד מפורש של הבדל המסלולים; לא תוקן בסבב הסקירה.

M2-05

GazeFollower — שלב 0 (נרמול/היפר־פרמטרים) וניסוי מאפייני ראש בכיול

בעבודה; ראו סעיף 11. שלב 0 בקוד ובבדיקה אוטומטית; טרם רץ מול מצלמה

M3-00

זהות מסך, הקפאה בשינוי תצוגה והתאוששות ללא ידיים

בעבודה; Tier 1 חלקית — הליבה, זיהוי EDID והחיווט למסך החי בוצעו; התאוששות ללא ידיים ואימות חומרה טרם

מה לעשות: המוצר קורא את המסך פעם אחת בהפעלה ולעולם לא שוב, ולכן שינוי רזולוציה, שינוי אחוז סקיילינג, סיבוב, ניתוק צג או גרירת החלון לצג אחר משאירים את המבט ממופה מול סרגל שכבר לא קיים — בלי שגיאה. בנוסף `screen_id` הוא חריץ חיבור ולא צג, ולכן החלפת צג באותו חיבור ובאותה רזולוציה עוברת את השוואת השוויון הקיימת. חוסם את M3-02: אין להזיז סמן אמיתי על סרגל לא מאומת.

Definition of Done: שינוי תצוגה מזוהה ומקפיא פלט מבט מיידית עם הסבר; נעילה שאינה משתחררת כשהתצוגה חוזרת לקדמותה; זהות מסך שאינה מסתמכת על שם ורזולוציה; מסלול התאוששות שאינו דורש ידיים ואינו דורש מבט מכויל; אימות ידני מול שני צגים.

ראיות ביצוע, 8.9.2026: נוצר `src/gazelink/display_watch.py` — ליבה נטולת Qt עם `describe_screen`, `identify_screen`, `DisplayIdentity` (שלוש דרגות ודאות), `detect_change`, `DisplayGuard` נועל, `DisplayWatcher` ו-`ensure_high_dpi_policy`. נוסף `ReasonCode.DISPLAY_CHANGED`. חמש בניות `ScreenGeometry` כפולות בחלונות אוחדו ל-`describe_screen` אחד (`grep "screen_id=screen.name()"` מחזיר 0). `gaze_window` מחווט במלואו: בדיקה בראש כל טיק, הקפאת פלט מבט וניקוי סמנים, `attach` ב-fullscreen ו-`detach` בסגירה. **הטיימר ולולאת הראייה ממשיכים לרוץ בכוונה** — עצירתם הורגת את האות שההתאוששות תלויה בו. בסקירה עצמית נתפס באג בגרסה הראשונה: `return` מוקדם דילג על `runtime.tick()`, כך שהטיימר רץ אך הפריימים לא נצרכו; תוקן כך שהפריים נצרך תמיד ורק פלט המבט נמנע. מדיניות PassThrough נקראת בכל חמש נקודות הכניסה לפני יצירת `QApplication`.

בדיקות: 41 בדיקות חדשות ב-`tests/test_display_watch.py`; הסוויטה המלאה 573 עברו (בסיס 532), 2 deselected. ruff check/format ו-mypy עברו על כל 39 הקבצים. `tests/test_gaze_window.py` עודכן לארגומנט החדש. הפער אושש בפועל ולא רק נטען: שתי `ScreenGeometry` של צגים שונים באותו חיבור ורזולוציה משוות `True`, וה-`DisplayIdentity` תופס זאת.

מגבלות ומה שלא נעשה: **לא הורצה מצלמה, לא הורץ Qt חי ולא נבדק שום צג פיזי** — כל האימות עד כה אוטומטי וללא חומרה. ארבעת המסכים האחרים (`calibration`, `test`, `validation`, `feature_check`) קיבלו רק את איחוד `describe_screen` ואת מדיניות ה-DPI, ועדיין אינם מחווטים ל-`DisplayGuard`. **התאוששות ללא ידיים לא מומשה**, וזה חוסם: ממצא בקוד מראה ש-`extract_eye_features` מחזיר `_invalid` כש-`lid_gap` מגיע לאפסילון, ולכן `openness` אינו זמין בעצימה מלאה והאות חייב להיות "פנים במעקב + גיאומטריית עין לא תקפה, מתמשך" — מנגנון שדורש פרוטוטייפ ומדידה על וידאו אמיתי, וחופף ל-M3-03. עד שזה קיים, ההקפאה היא מלכודת למשתמש חסר גפיים. בדיקת התאמת פרופיל בטעינה (`measure_feature_drift` הקיים) טרם חוברה. אין commit.

תיקוני סבב שני, 8.9.2026 — שני מסלולי כשל שוחזרו ותוקנו: (1) **קריטי** — זיהוי השינוי השתמש ב-`matches()`, שדורש סידורי EDID מאומת. לפאנל בלי EDID (כל לפטופ) התשובה ל"האם השתנה?" הייתה תמיד כן, ולכן המבט קפא מיידית וצמיתות בלי ששום דבר השתנה. הופרדה `differs_from()` להשוואת מפתחות עבור זיהוי שינוי, בעוד `matches()` נשארת לשאלת "מותר לטעון פרופיל שמור" בלבד. (2) `poll()` החזיר את הנעילה הריקה כש-Qt מחזיר `None` למסך, ולכן **צג מנותק לא הקפיא ולא הודיע** — תרחיש 4 ברשימת האימות. נוסף `trip_display_lost()` ושדה `display_lost`. שתי ההתנהגויות שוחזרו לפני התיקון ואומתו אחריו, ונוספו שש בדיקות רגרסיה כולל בדיקה שהתיקון לא קנה שקט על חשבון ביטול הזיהוי. בדיקה קיימת אחת קיבעה את ההתנהגות הבאגית ותוקנה. הסוויטה: 579 עברו.

סבב שלישי — שני הפריטים שנותרו מהשלב הראשון, 8.9.2026:

(א) **מנגנון התאוששות ללא ידיים** — נוצר `src/gazelink/recovery_gesture.py`: `EyeCloseDetector`, מכונת מצבים טהורה על שעון מונוטוני, ו-`eyes_unreadable_with_face_present` שמגדיר את האות כ"פנים במעקב + שתי העיניים ללא גיאומטריה תקפה" ולא כ-`openness` נמוך, מהסיבה שתועדה קודם. עצימה קצרה = מעבר לאפשרות הבאה, ארוכה = אישור; `CONFIRM` נורה בזמן שהעיניים עדיין עצומות ולא בשחרור, כדי שהמשתמש לא יצטרך לנחש מתי לפקוח. הספים נלקחים מ-`GestureTimingConfig` הקיים; נוסף שם `recovery_confirm_ms=1500` עם ולידציה שהוא גדול מ-`intentional_hold_min_ms`. באג שנתפס בסקירה עצמית תוך כדי כתיבת הבדיקות: "שחרור" ו"אובדן פנים" הגיעו שניהם כ-`eyes_closed=False`, ולכן **הפניית ראש באמצע החזקה שידרה `CYCLE`** — פעולה שאיש לא ביקש, מערוץ הקלט היחיד שנשאר בהקפאה. הופרד `face_present` והאובדן מבטל בשקט.

(ב) **חיווט ההגנה לכל חמשת המסכים** — `gaze_window`, `calibration_window`, `validation_window`, `feature_check_window` ו-`test_window` מחזיקים כולם `DisplayWatcher` עם `poll` בראש הטיק ו-`detach` בסגירה. הבחנה מכוונת בהתנהגות: במסך המבט החי הפריים ממשיך להיצרך ורק הפלט נמנע, כי שם ההתאוששות רלוונטית; במסכי המדידה והכיול האיסוף **מבוטל** — דגימות שנוקדו מול גיאומטריה ישנה לא ייכנסו לאותו dataset עם חדשות. ב-`test_window` הבדיקה הרציפה נוספה לצד `_verify_surface_matches_ruler` הקיימת, שנורית רק פעם אחת בכניסה למסך מלא ולכן הייתה עיוורת לשינוי באמצע איסוף.

בדיקות: 26 חדשות ב-`tests/test_recovery_gesture.py` (דחיית מצמוץ טבעי, ירי יחיד, ביטול באובדן פנים, cooldown, ספים מקונפיגורציה). סוויטה מלאה **605 עברו**. ruff ו-mypy נקיים על 40 קבצים.

**מה שעדיין חסר להשלמת השלב הראשון:** מכונת המצבים קיימת אך **אינה מחוברת לשום ממשק** — אין עדיין מסך התאוששות עם אפשרויות, ולכן המחווה מזוהה אך לא עושה דבר. הספים (400/1500 מ"ש) **לא תוקפו מול משתמש או וידאו אמיתי** — הם נומקו ולא נמדדו. ולא נבדק שום צג פיזי: כל האימות עד כה אוטומטי.

סבב רביעי — שני באגים בשילוב ההקפאה, 8.9.2026. שניהם נבעו מעוזר גנרי אחד שנכתב לשלושת החלונות ולכן הופיעו בשלושתם:

(1) **פלט ישן נשאר על המסך.** ה-`_freeze_for_display` שנוצר רק עצר את הטיימר; `grep` ל-`_target.hide()` בשלושת החלונות החזיר אפס. מטרת הכיול/האימות נשארה מצוירת במיקום שחושב מהגיאומטריה הישנה — בדיוק הפלט הסביר-אך-חסר-משמעות שהפיצ'ר קיים כדי למנוע.

(2) **Restart הסתובב בשקט.** `_on_restart` מפעיל מחדש את הטיימר; הטיק הבא זיהה את הנעילה וקרא ל-`_freeze_for_display`, שיצא מיד כי הדגל כבר היה True — **בלי לעצור את הטיימר**. התוצאה: מסך שמסתובב לנצח, לא מצייר דבר ולא מסביר דבר, אחרי שהמשתמש לחץ Restart.

התיקון: ההקפאה נעשתה אידמפוטנטית **בהשפעה** ולא ב"דילוג" — עצירת טיימר והסתרת מטרה קורות בכל קריאה, והדגל שומר רק על כך שההודעה תיאמר פעם אחת. בנוסף `_on_restart` ו-`_on_retry` בכיול חוסמים מראש כשההקפאה פעילה ומסבירים שהפעלה מחדש של האפליקציה נדרשת. אותו טיפול הוחל על `test_window`.

בדיקות: שלוש בדיקות רגרסיה נוספו (`test_freezing_hides_the_target_not_only_stops_the_clock`, `test_a_restart_after_a_freeze_cannot_leave_the_timer_running`, `test_the_explanation_is_given_once_however_often_the_freeze_runs`) עם fakes ובלי Qt. סוויטה מלאה **608 עברו**. ruff ו-mypy נקיים על 40 קבצים.

**שינויים שאינם של סבב זה:** נמצאו שינויים לא-מתוזמנים ב-`gazefollower/` (‏475 שורות בארבעה קבצים, ושני קבצים חדשים `gf_vertical.py` ו-`tests/test_vertical.py`) שלא נוצרו בעבודה הזו ולא נגעו בהם. יש לבדוק אותם בנפרד לפני כל commit.

סבב חמישי — תיקון חלקי ובדיקות חסרות ערך, 8.9.2026:

(1) **הבדיקות של סבב 4 לא נגעו בקוד ייצור.** נכתבה מחלקה `_FreezableWindow` בתוך קובץ הבדיקה שחיקתה את לוגיקת ההקפאה. ביטול התיקון האמיתי היה משאיר אותן ירוקות — ביטחון שקרי, גרוע מהיעדר בדיקה.

(2) **תיקון הפלט הישן היה חלקי.** `gaze_window` הסתיר `_raw_dot` ו-`_corrected_dot` אך לא את `_target_dot`; `calibration_window` הסתיר `_target` אך לא ניקה את `_prediction_overlay`. שניהם נשארו מצוירים מול גיאומטריה שכבר לא קיימת.

התיקון לשניהם יחד: חולצה `OutputFreeze` ל-`display_watch.py` — מחלקת ייצור אמיתית שכל חמשת החלונות מפעילים, כל אחד עם רשימת החלקים שלו. כל פעולה רצה בכל קריאה, `engage()` מחזיר True רק בפעם הראשונה, וכשל בפעולה אחת לא מונע מהשאר לרוץ (מסך מוקפא-למחצה עם סמן ישן הוא בדיוק הכשל). הבדיקות נכתבו מחדש מול המחלקה האמיתית, ונוספה בדיקה שמאמתת שכל חמשת החלונות אכן מאצילים אליה — כי בלעדיה הבדיקות האחרות שוב לא מבטיחות דבר על ההתנהגות בפועל.

אימות שהבדיקות אכן תופסות: התיקון בוטל ידנית בקוד והוחזר — `test_freeze_reasserts_itself_after_something_restarts_the_timer` נכשלה בזמן שהבאג היה נוכח ועברה אחרי ההחזרה. סוויטה מלאה **611 עברו**. ruff ו-mypy נקיים על 40 קבצים.

**דפוס חוזר שראוי לתעד:** ארבעה סבבים רצופים, וכל הבאגים מאותה משפחה — לוגיקת "כבר טיפלנו בזה" ששוברת את הפעם הבאה: `matches()` שענה על שאלה אחרת, `return` מוקדם שדילג על צריכת פריים, דגל שדילג על עצירת טיימר, ובדיקה שבדקה עותק במקום את המקור. הקוד מעולם לא רץ מול מסך פיזי; הבדיקות האוטומטיות תופסות רק את מה שנחשב מראש.

סבב שישי — חיבור המחווה לפעולה, 8.9.2026:

**פריט 2 בדיווח (עצירת טיימר שהורגת את המצלמה) נבדק ונמצא לא-נכון לגבי החלון החי.** ב-`gaze_window` ה-`OutputFreeze` מכיל שלוש הסתרות סמנים בלבד, בלי `timer.stop`, והטיק צורך `runtime.tick()` לפני בדיקת ההקפאה — המצלמה ממשיכה. עצירת הטיימר קיימת רק במסכי המדידה. **אך יש שם נקודה פתוחה אמיתית**: `calibration_window` הוא מסך שמשתמש קצה מריץ, ושם העצירה כן יוצרת מלכודת אם התצוגה משתנה באמצע כיול. לא טופל בסבב זה.

**חיבור לממשק (פריט 1) — בוצע.** נוספו `RecoveryOption` ו-`RecoveryMenu` ל-`recovery_gesture.py`: לוגיקה טהורה של תפריט קטן שמונע לחלוטין בעפעפיים, עם ההדגשה מתחילה על האפשרות הפחות הרסנית כדי שעצימה ארוכה מקרית לא תעשה משהו דרסטי. `gaze_window` מחווט: בטיק מוקפא הוא מזין את הגלאי מ-`tick.observation` הגולמי, `CYCLE` מזיז הדגשה, `CONFIRM` מבצע. שתי אפשרויות: כיול למסך הנוכחי, או יציאה. הבחירה מוחזרת לקורא דרך `RECALIBRATE_REQUESTED_EXIT_CODE = 10`, כי החלון עצמו אינו יכול לכייל — המודל שלו שייך למסך שנעלם.

בדיקות: תשע נוספו (התנהגות התפריט, ולידציה, ובדיקת חיווט שמאמתת ש-`gaze_window` אכן קורא ל-`eyes_unreadable_with_face_present(observation)` ומאציל ל-`_act_on_recovery` — מחווה שמזוהה ולא עושה כלום אינה התאוששות). סוויטה מלאה **620 עברו**. ruff ו-mypy נקיים על 40 קבצים.

נכתב `docs/LIVE_DISPLAY_TEST.md` — בדיקה חיה אחת, שישה חלקים, כולל כיוון משכי העצימה. **הספים 400/1500 מ"ש הם השערה ולא מדידה**; זה מה שהבדיקה החיה אמורה לספק.

**עדיין חסר להשלמת השלב:** אין אימות חומרה כלשהו. `calibration_window` עוצר את הלולאה בהקפאה ולכן חסר שם מסלול התאוששות. הספים לא מכוילים.

סבב שביעי — שני כשלים חוסמים בחיווט ההתאוששות, 8.9.2026:

(1) **אדם שני בפריים הפעיל פעולה.** נכתבו שתי הגדרות שונות ל"פנים נוכחות": `eyes_unreadable_with_face_present` פוסלת `MULTIPLE_FACES`, בעוד אתר הקריאה ב-`gaze_window` פסל רק `LOST`. עובר אורח שנכנס לפריים באמצע החזקה נקרא כ"פקח עיניים" והשלים מחווה של מישהו אחר — מערוץ הקלט היחיד שנשאר בהקפאה. שוחזר בהרצה בפועל.

(2) **"כייל למסך הזה" לא כייל כלום.** `gaze_window` החזיר `RECALIBRATE_REQUESTED_EXIT_CODE`, ו-`app.py` פשוט החזיר אותו הלאה: החלון נסגר, התהליך יצא, ושום כיול לא הופעל. האפשרות הבטיחה פעולה ולא ביצעה אותה — כלומר בדיוק כשל ההתאוששות שסבב 6 טען שתיקן.

תיקון: חולצה `advance_recovery()` ל-`recovery_gesture.py` — פונקציית ייצור שמרכיבה את שתי השאלות פעם אחת, כי הבאג היה **בהרכבה** ולא באף אחת מהן; `gaze_window` מאציל אליה. ב-`app.py` הקוד מחווט ל-`run_guided_calibration` על המסך הנוכחי.

**הבדיקות הראשונות שכתבתי לשני הבאגים לא תפסו אותם.** הראשונה הרכיבה את הקריאה בעצמה במקום לקרוא לקוד הייצור; השנייה סרקה מחרוזת שמופיעה ממילא במקום אחר באותו קובץ. שתיהן נכתבו מחדש: הראשונה עוברת דרך `advance_recovery`, השנייה מריצה את `cli(["--gaze-check"])` האמיתי עם שני המסכים מזויפים ומאמתת את סדר הקריאות. **אומת בפועל**: שני הבאגים הוחזרו לקוד ושתי הבדיקות נכשלו, ואז הוחזרו והכל ירוק. סוויטה **626 עברו**.

**זה הסבב השלישי שבו בדיקה שכתבתי לא בדקה כלום.** הדפוס עקבי: בדיקה שמרכיבה מחדש את הלוגיקה במקום לקרוא לה. כל בדיקת רגרסיה חדשה חייבת לעבור אימות "החזר את הבאג וראה אותה נכשלת" לפני שהיא נחשבת.

סבב שמיני — הכיול פתח על הצג הלא-נכון, 8.9.2026:

האפשרות אומרת "כייל **למסך הזה**", אבל `run_guided_calibration` פתח תמיד על `primaryScreen()`. תרחיש הכשל הוא בדיוק זה שהבדיקה החיה מתארת: גררת את החלון לצג השני, הוא קפא, בחרת לכייל — והכיול נפתח על הצג הראשון **ודיווח שהצליח**. משתמש מקבל מודל למסך שהוא לא מסתכל עליו. זה גרוע מסירוב.

תיקון: נוספה `pick_screen()` ל-`display_watch.py` — בוחרת צג לפי שם, ונופלת חזרה ל-primary עם הודעה מפורשת אם הצג נותק בין הבקשה לביצוע. `run_guided_calibration` מקבל `screen_name`. `gaze_window` זוכר את הצג שהמשתמש היה עליו בפועל בעת הבקשה ומעביר אותו דרך `on_recalibration_request` — **קולבק מפורש ולא גלובל מודולי**, שנפסל כ-hidden global state לפי כללי הפרויקט.

הערה: `application.primaryScreen()` נעלם לגמרי מ-`calibration_window`, וזה גם צעד ראשון לעבר Tier 3 — משתמש שצג העבודה שלו הוא המשני יכול עכשיו לכייל אותו.

בדיקות: חמש נוספו (`pick_screen` לפי שם, ברירת מחדל, נפילה חזרה בניתוק, ושתי בדיקות חיווט — אחת שמאמתת ש-`calibration_window` אינו קורא ל-`primaryScreen`, ואחת שמריצה את ה-CLI האמיתי ומאמתת ש-`screen_name` שורד את המעבר). **אומת לפי הכלל שנקבע בסבב הקודם**: הבאג הוחזר, שתי הבדיקות נכשלו, הוחזר התיקון והכל ירוק. סוויטה **631 עברו**.

`docs/LIVE_DISPLAY_TEST.md` עודכן: סעיף 6 דורש עכשיו במפורש לגרור לצג השני לפני בחירת הכיול, כי פתיחה על הראשי היא הכשל שנבדק.

סבב תשיעי — הצג הנבחר לא הגיע לווידג'ט, 8.9.2026:

**התיקון של סבב 8 יצר כשל גרוע מהמקורי.** `run_guided_calibration` בחר נכון את הצג לגיאומטריה, אבל `show()` המשיך לקרוא `self._widget.screen()` — הצג ברירת המחדל. התוצאה: מטרות שחושבו ל-2560×1440 מצוירות על 1920×1080, כלומר הסרגל והמשטח מסכימים פחות מאשר לפני התיקון, ושניהם נראים תקינים. אותה תקלה נמצאה גם ב-`gaze_window`.

הלקח הזה כבר היה כתוב בקוד — `test_window.py` שורה ~425 מתעד אותו במפורש ("The SAME QScreen the recorded geometry came from") — וגם בתוכנית שאושרה, שדרשה לתפוס QScreen אחד ולהשתמש בו לשניהם. לא יושם.

תיקון: ה-QScreen שנבחר מועבר לתוך `_CalibrationWindow` ו-`_GazeCheckWindow` ונשמר כ-`self._screen`, ו-`show()` בשניהם מודד לפיו. אומת לפי הכלל: הבאג הוחזר, הבדיקה נכשלה, הוחזר התיקון והכל ירוק. סוויטה **632 עברו**.

**מצב הסדרה: תשעה סבבים, תשעה באגים, אפס דקות מול חומרה.** שלושה מהם היו רגרסיות שהוכנסו בזמן תיקון הבאג הקודם. ארבעה נמצאו רק אחרי שנטען שהקוד עובד. שלוש בדיקות רגרסיה שנכתבו לא בדקו כלום עד שנאכף עליהן כלל האימות. אין בסיס לטעון שהמסלול המלא עובד לפני בדיקה חיה.

סקירה: לבקשת המשתמש הסקירה עוברת דרכו ולא דרך Codex בעבודה זו. הממצאים לעיל אותרו בבדיקה עצמאית של הקוד ולא התקבלו כנתון.

M3-01

הפעלה בטוחה של קלט Windows

לא התחיל; עצירה, השבתה ושחרור פעולות במצבי תקלה

ראיות סקירה, 7.9.2026: קוד העכבר של Eyecom נמצא בלתי מתאים לשילוב ישיר; תועדו הצעות לבקרת בריאות מצלמה ולשערי הפעלה, וכן דרישה לא להסתמך רק על מצב TRACKED בעת debounce. לא מומש קלט OS; ראו סעיף 12.

M3-02

הזזת הסמן לפי מבט

לא התחיל; גבולות מסך, רגישות, מצב הדמיה וזמן תגובה

M3-03

מצמוץ, קריצה ושהייה

לא התחיל; הבחנה מפעולה טבעית, תזמון ומניעת הפעלה כפולה

M3-04

פעולות עכבר והדגמה

לא התחיל; לחיצות, גרירה, עצירה וחידוש

M4-01

הקלה על בחירת מטרות

לא התחיל; הגדלה, הצמדה, זום, גלילה ומשוב

M4-02

מקלדת על המסך

לא התחיל; עברית ואנגלית, בחירה והקלדה

M4-03

פרופילים והגדרות

לא התחיל; כיול אישי, רגישות ושפה

ראיות סקירה, 7.9.2026: מסך הפרופיל של Eyecom מתאים להשראה מעל ProfileConfig הקיים, אך הגדרות המחוות שלו אינן מחוברות לשירות. לא הוטמעו UI או אחסון חדשים; ראו סעיף 12.

M4-04

בדיקת שימוש ראשונית

לא התחיל; גלישה, גלילה, הקלדה, עייפות וטעויות

M5-01

חוזי חיבור והרשאות

לא התחיל; גרסאות API והרשאות גישה

M5-02

העברת אירועים לאפליקציות

לא התחיל; ערוץ מקומי, WebSocket ו־REST

M5-03

ערכת פיתוח ודפדפן

לא התחיל; דוגמת חיבור ושילוב בדפדפן

M5-04

PANDY / LAXI

חסום חלקית; פרטי חיבור וגישה מגורמים חיצוניים

M6-01

אמינות במגוון תנאים

לא התחיל; חומרה, תאורה, משקפיים, תנועת ראש ובדיקות ממושכות

M6-02

התקנה ותחילת שימוש

לא התחיל; בדיקת סביבה, אשף, התקנה ועדכונים

M6-03

ניהול משתמשים ונתונים

לא התחיל; הרשאות, גיבוי, אבטחה וכלי מטפל

M6-04

גרסה להפצה ופיילוט

לא התחיל; החלטות מוצר, רישוי ותוכנית פיילוט פתוחות

7. מה במסמך הישן יצר בלבול

הניסוח או הממצא הישן

איך לקרוא אותו עכשיו

כותרת ״טרם אומת מול מצלמה״ ומאוחר יותר ״אומת מול מצלמה״

נרשמו עדכונים לאורך זמן בלי להחליף את הכותרת. יש לציין בנפרד מה בדיוק אומת: הפעלה, כיול או דיוק

כמה סעיפים בשם ״הצעד הבא״

אלה צעדים מתקופות שונות. סדר העבודה הפעיל הוא A–E במסמך הזה

הוראות לחזור ל־gaze-feature-check ולפיתוח מודלים

שייכות למסלול העצמאי, שמושהה בהחלטה הנוכחית

גרסאות שונות של נתוני העיניים ושל ספי האיכות

היסטוריית שינויים. אין להעתיק סף ישן לקוד בלי לבדוק מה הגרסה הפעילה

הצהרה שבמצב לא TRACKED הספרייה אינה נקראת, לצד תיקון שמזין אותה בכל פריים

סתירת תיעוד שיש ליישב מול הקוד. התיקון המאוחר מתאר הפרדה בין הזנת תמונות לבין אישור פלט

מאות בדיקות שעברו

ראיה לבדיקות המסוימות שנעשו באותה גרסה, ולא ציון דיוק המבט

השערה על תנועת ראש שהוחלפה בהשערה על מעט דגימות

הסיבה לכשל אינה מוכחת רק מכך שנכתבה מסקנה חדשה. יש לשמור מדידה והשערה בנפרד

זמני עיבוד, FPS ושגיאות מריצות שונות

אין לאחד אותם למדד אחד של המערכת הנוכחית

במסמך מתועדים גם תיקונים ממשיים: דגימות שאושרו בלי נתוני עיניים, כיול שאסף מוקדם מדי, התנגשות טעינת MediaPipe, חלון שלא התרענן ומצלמה שנתפסה בידי תהליך אחר. אלה מסבירים מדוע נדרשה עבודת אינטגרציה משמעותית; הם אינם מוכיחים שכל בעיות הדיוק נפתרו.

בהרצה מסוימת תועדו רק 61 פריימים מאושרים מתוך 333. זה ממצא חשוב על אותה הרצה; יש למדוד מחדש במסלול הנוכחי ולא להציג 18% כתכונה קבועה של הספרייה.

הוזכרו גם כ־361–392 פיקסלים בניסוי חיצוני מול חציון של כ־438 במנוע העצמאי. אלו מספרים היסטוריים עם תנאי ניסוי ועיבוד שיש לבדוק, ואינם מוכיחים את דיוק EyeGestures בתוך המערכת הנוכחית.

8. החלטות שעדיין צריך להסדיר

נושא

המצב

חומרת ייחוס

לאשר דגם מצלמה ולתעד מסך, רזולוציה, מיקום מצלמה ומרחק ישיבה

סביבת פיתוח

Python/Windows הם הכיוון הנוכחי; לבדוק אם סעיף D-02 הישן עוד דורש החלטה נוספת

מסך יחיד

במסמך מופיע תחום עבודה של מסך יחיד עד M4; לתעד כהגבלת הגרסה הרלוונטית

הצלחת המוצר

לקבוע משימות, גודל מטרות, שיעור הצלחה, זמן תגובה והפעלות שגויות מותרים

ספי הניסוי הישנים

120px לשגיאת בדיקה ו־50px לפער כיול מופיעים ככללים לניסוי קודם; אין להחילם אוטומטית על EyeGestures או לשנותם בשקט

מחוות ופעולות

ברירות מחדל לקריצה/שהייה ופעולת עצירה

משתמש ומטפל

הרשאות, בעלות על פרופיל ושינוי הגדרות

חיבורים חיצוניים

חוזי PANDY/LAXI וגישה

נתונים ותמיכה

מדיניות לוגים, אבחון ושמירת מידע

הפצה ורישוי

במסמך המקורי מסומן נושא רישוי פתוח ל־EyeGestures לפני הפצה. לשמר אותו כמשימת בירור; השכתוב אינו מאמת מסקנה משפטית

פיילוט

אוכלוסיית בדיקה, הסכמה, תנאים ומדדי הצלחה

אין צורך לפתור עכשיו את כל החלטות הגרסה הסופית. הדרישות של ניסוי הדיוק הקרוב צריכות להיות מפורשות לפני שמכריזים שהוא עבר.

9. איך לעדכן את המסמך מעכשיו

לכל משימה פעילה יש לשמור רק: מטרה, מצב נוכחי, מה עושים עכשיו, תנאי סיום, ראיות וחסמים.

להבחין בין שלושה מצבים: הקוד נכתב, עבר בדיקה אוטומטית, נבדק מול מצלמה ומשתמש. לסמן יכולת כשלמה רק כאשר בוצע סוג האימות הנדרש עבורה.

תוצאות ישנות, רשימות קבצים, מספרי בדיקות והחלטות שבוטלו נשמרים בהיסטוריה. בראש תוכנית העבודה מציגים רק את ההחלטה התקפה ואת הבדיקה הבאה.

לפני שינוי קוד, לקרוא את הוראות הריפו והסעיפים הרלוונטיים באפיון. לבדוק את הקוד העדכני לפני הצגת משימה כחדשה או כגמורה. אם ההוראות הישנות סותרות החלטה חדשה מהמשתמש, לתעד את העדכון במפורש.

10. ראיות ביצוע — 6 בספטמבר 2026

מה נעשה בסבב הזה, לפי שלושת המצבים שסעיף 9 מגדיר.

עבר בדיקה אוטומטית, לא נבדק מול מצלמה:

מסלול EyeGestures מלא: כיול 36 נקודות, הקפאה מאומתת, מדידה על מטרות מוחזקות, כתיבת שני קבצי תוצאות.

הפקודה: python -m gazelink --gaze-test --engine eyegestures, ואחריה analyze.py --predictions על הקובץ שנכתב.

הקובץ הראשי מכיל את כל התחזיות התקפות של הספרייה. קובץ נלווה בסיומת gated מכיל את אותן דגימות כשהשער שלנו הוא המסנן. שניהם נכתבים תמיד, ואף אחד מהם אינו ברירת מחדל שקטה. הסיבה: כלי הניתוח מסתיר דגימות לא מאושרות כברירת מחדל, ולכן רישום פסק הדין של השער בשדה הזה היה מוחק את רוב הפלט של הספרייה מהמדידה של עצמה.

527 בדיקות אוטומטיות עוברות. ruff ו-mypy נקיים.

לא נבדק כלל:

דיוק EyeGestures. אין עדיין שום מספר. כל הבדיקות למעלה מוכיחות שהצנרת מודדת את מה שהיא מציגה, לא שהמנוע מדויק.

עלות המעבד של שתי הרצות זיהוי פנים במקביל.

האם תהליכוני הכיול מצטברים בסשן ארוך.

הערה להשוואה: המספר שייצא מההרצה הבאה אינו בר-השוואה ל-508.7 פיקסלים שנמדדו קודם. המטרות נוצרות עכשיו ביחס לרשת אחרת, ולכן זה סרגל אחר וקו בסיס חדש.

11. M2-05 — GazeFollower: שלב 0 (נרמול/היפר־פרמטרים) וניסוי מאפייני ראש בכיול

עדכון סקירה ותכנון, 7.9.2026: לבקשת המשתמש נבדק מסלול הנקודה הכחולה בהרצת round6/T1 עם round5/models/today. התחזית המכוילת מצוירת ללא החלקה זמנית; המסנן בפרויקט הראשי אינו מחובר למסלול הזה. במטא־נתוני ההרצה: 905 פריימים, 32.258 FPS חציוני, finished=true וללא שגיאות subscriber. נתונים אלה אינם מדידת דיוק או רעד. קיימת אי־התאמה לתיאור הסביבה החיצונית ולהערת "טרם רץ" בהמשך: הכלים והקלטת round6 קיימים כעת בתיקיית gazefollower המקומית. לא שונתה החלטת מנוע המוצר. הוכנה [תוכנית ייצוב](docs/gazefollower-overlay-filter-plan.md): One Euro כמועמד ראשון, השוואה ל-EMA/Kalman, הפרדת כוונון מ-T1, טיפול באובדן מעקב ומדידת רעד מול השהיה. הסקירה ידנית של קוד/בדיקות קיימות/מטא־נתונים; לא נטענו embeddings, לא הורצו בדיקות או מצלמה ולא מומש מסנן. קבצים ששונו בסבב: התוכנית ו-TASKS.md בלבד, ללא commit. סטטוס המשימה נשאר בעבודה; What to do ו-Definition of Done נשמרו. נותרו benchmark ואימות חי לפני טענת שיפור.

עדכון ביצוע ייצוב, 7.9.2026: נוסף `gf_gaze_filter.py` עם OFF/EMA/One Euro/Kalman בזמן אמיתי וביחידות פיקסל, reset בפער/זמן לא־תקין וללא תלות במצלמה או ב־OS. `gf_record.py` מסנן כברירת מחדל את הנקודה הכחולה אחרי המודל המכויל, פעם אחת לכל פריים; מצמוץ, tracking loss, ערך לא־סופי ופער של 250ms מסתירים אותה ומאפסים היסטוריה. אפשר לראות תחזית מכוילת לא־מסוננת בסגול או לכבות דרך CLI. ההגדרות נשמרות במטא־נתוני ההקלטה.

הבחירה נעשתה ב־`gf_filter_benchmark.py` על round2/TUNE בלבד מתוך OFF, EMA, One Euro ו־Kalman, תחת שערי שינוי של עד 5% בחציון/P95 שגיאה ו־100ms בבדיקת מדרגה סינתטית. נבחר One Euro: min_cutoff=1.2Hz, beta=0.0005 Hz/(px/s), מדרגה 100ms. T1 של round6 נמדד פעם אחת לאחר הבחירה: רעד P95 380.81→219.04px (42.5%-), קפיצת פריים P95 147.80→63.49px (57.0%-), שגיאה חציונית 209.09→192.45px, P95 שגיאה 747.82→673.03px, כיסוי 100%, ועלות מסנן כ־0.0019ms לפריים. ב־TUNE רעד P95 ירד 116.33→86.30px (25.8%); יעד הניסוי המוצע של 50% לא הושג ולכן אינו PASS. המדדים מצטברים בלבד; embeddings נשארו תחת recordings. הדוח: [gazefollower-filter-results.md](docs/gazefollower-filter-results.md).

בדיקות ואימות: 61 בדיקות ממוקדות עברו; ruff check/format עברו בארבעת הקבצים החדשים; F-check עבר ב־gf_record ובבדיקת החיבור; compileall עבר. dry-run דרך GazeFollower האמיתי עם רכיבי מצלמה/פנים/מודל מזויפים עבר: T1 הסתיים, 31.25 FPS, אפס שגיאות, watchdog כבוי וניקוי library_release/stop_sampling תקין; לא הופעל קלט OS. הסוויטה המלאה הריצה 302 בדיקות: 301 עברו בסביבת ההערכה ואחת קיימת ב־test_m0 נכשלה כי מערכת ההפעלה לא דיווחה רוחב מסך פיזי, ולכן הבדיקה לא יכלה לייצר את האזהרה שהיא מצפה לה. אין קשר למסנן. לא הופעלו מצלמה אמיתית או משתמש. קבצים ששונו: gf_gaze_filter.py, gf_filter_benchmark.py, gf_record.py, שלוש בדיקות, RUN_PHASE0.md, שני דוחות התכנון/תוצאה ו־TASKS.md. לא נוצר commit. M2-05 נשאר בעבודה עד בדיקה חיה; What to do ו־Definition of Done נשמרו.

המשך ואימות סופי לפני הרצה חיה, 7.9.2026: נבדקו העריכות האחרונות ב־`gf_record.py` וב־`tests/test_record_logic.py` שבוצעו ממש לפני הפסקת הסבב הקודם. אין תהליך Python תקוע מסבב ה-dry-run הקודם (round99); `T1.meta.json` מראה `integrity.finished=true`, `aborted=false`, `watchdog_tripped=false`, `subscriber_errors=0` — סיום נקי ולא רק קובץ מטא־נתונים חלקי. נבדק קוד ה־wiring בפועל (`gf_record.py` שורות ~405-431): המסנן מתעדכן פעם אחת לכל פריים חדש (לא לכל ציור), התחזית הגולמית והמסוננת נשמרות בנפרד ב־RunnerState, ומצמוץ/אובדן מעקב/תחזית לא־סופית/כשל חיזוי מאפסים את היסטוריית המסנן באותו פריים; בדיקת עדכניות (`visible_overlay_point`, `OVERLAY_STALE_S`) מסתירה נקודה ישנה גם כשפריימים חדשים מפסיקים להגיע. הרצת הסוויטה המלאה כעת: 304 בדיקות עברו (כולל שמונה בדיקות ייעודיות למסלול ה-overlay/filter ב־test_record_logic.py ו־test_m0 שעבר הפעם — כשל ה-DPI הקודם היה תלוי-סביבה ולא בעיה בקוד). ruff check/format עברו שוב על ארבעת הקבצים החדשים (0 ממצאים); compileall עבר. הרצת benchmark חוזרת עם אותם פרמטרים הניבה תוצאות זהות עד רעש נקודה־צפה לדוח הקיים (רעד P95 T1 380.81→219.04px, קפיצת פריים P95 147.79→63.49px, שגיאה חציונית 209.10→192.45px, כיסוי 100%/100% משני הצדדים — אין נשירת דגימות קשות מהמדידה). לא בוצע commit; קבצים חדשים בלבד מהסבב הקודם נשארו untracked. **M2-05 עדיין אינו DONE**: יעד הניסוי של 50%- ברעד לא הושג (25.8% ב-TUNE, 42.5% ב-T1), ואין עדיין אימות חי מול מצלמה — שני התנאים נדרשים לפי ה-Definition of Done. הפקודה הבאה המוצעת: `gf_record.py --round 7 --protocols T1 --x-range 0.3 0.7 --overlay-model recordings\round5\models\today --overlay-show-unfiltered` עם אותה גיאומטריה (round7 פנוי, round6 לא נדרס).

**Codex QA חסום**: סקירת ה-Stop gate נכשלה עם פלט ריק; בדיקת `task-resume-candidate` חשפה את הסיבה בפועל — Codex הגיע למגבלת השימוש שלו ("You've hit your usage limit... try again at 6:00 PM"), לא תקלה חולפת. לפי סעיף 7.5 לא בוצע ניסיון חוזר עיוור ולא הושבת השער; הסקירה הסופית לפני סימון DONE נרשמת כחסומה עד שהמכסה מתחדשת (סביב 18:00, 7.9.2026). כל שאר הבדיקות בסבב זה (סוויטת בדיקות, ruff, compileall, שחזור benchmark) הן ולידציה עצמאית שלי, לא תחליף לסקירת Codex. הערה נלווית: לאורך הסבב הזה חוזרות רשומות job תקועות במצב "starting" ב-`/codex:status` (למשל task-mtr3kzep-3qozya, task-mtr3vq1h-utyunr, task-mtr3zwum-qjhkmx) שאינן נענות ל-cancel ("no job found") בעוד כל ניסיון אחר נכשל תוך כ-5 שניות באותה שגיאת מכסה — נראה כתקלה בספר החשבונות של הפלאגין עצמו, לא בקוד הפרויקט; לא טופל מעבר לדיווח.

אימות חי ראשון מול מצלמה, 7.9.2026: המשתמש הריץ שלוש הקלטות T1 עם אותה גיאומטריה/מודל (round7 עם ברירת המחדל min_cutoff=1.2/beta=0.0005, round8 עם הגדרה כבדה min_cutoff=0.4/beta=0 ו-`--overlay-show-unfiltered` להשוואה חזותית, round9 חוזר על ההגדרה הכבדה בלי הצלב הסגול). משוב המשתמש: round7 — "עדיין יש רעד, קצת יותר טוב אבל צריך להיות ממש יציב"; round8 — "הנקודה הכחולה יציבה" (הצלב הסגול המפריע זוהה כ-`overlay_raw_norm` הבלתי מסונן, מוצג בכוונה רק עם `--overlay-show-unfiltered`, לא באג); round9 — "נראה מעולה". שלוש ההקלטות נבדקו כ-`integrity.finished=true`, `aborted=false`; round9 הריץ 903 פריימים ב-32.26 FPS חציוני עם `overlay_filter.one_euro_min_cutoff_hz=0.4`, `one_euro_beta_hz_per_px_s=0.0` במטא־נתונים — תואם את מה שהמשתמש ראה. זהו דיווח סובייקטיבי-חזותי בלבד; לא חושבו רעד/דיוק כמותיים על round9 (לא הורץ `gf_fit.py`/`gf_filter_benchmark.py` על ההקלטה הזו). ניתנה המלצה למשתמש שלא להמשיך לכוונן את המסנן מעבר להגדרה הכבדה — הסריקה על round2/TUNE לא הראתה שיפור משמעותי נוסף מתחת ל-min_cutoff=0.4/beta=0, ורק פיגור תגובה גדל (~1 שנ' ל-90% מתנועה גדולה) — והוצע לעבור לבדיקת דיוק/היסט של המודל (חציון ~200px, P95 ~700px שגיאה על round6/T1, בלתי מושפע מסינון) כשלב הבא, בנפרד משאלת הרעד. **M2-05 עדיין אינו DONE**: יש עכשיו אישוש חזותי חי ראשון להגדרה הכבדה, אך אין עדיין מדידת רעד/דיוק כמותית על round9, אין הגדרת ברירת מחדל עדכנית בקוד (הקוד עדיין ברירת מחדל ל-min_cutoff=1.2/beta=0.0005 שנבחר ב-benchmark המקורי), ו-Codex QA הסופי עדיין חסום.

חיפוש מסנן אדפטיבי ותוצאה שלילית מדידה, 7.9.2026: לבקשת המשתמש נבדק אם `beta` חיובי יכול להחזיר את זמן התגובה בלי לוותר על היציבות של ההגדרה הכבדה. שינויי קוד: `ONE_EURO_PRESETS` ב-`gf_gaze_filter.py` (שמות מפורשים ל-"responsive" 1.2/0.0005 ול-"high-stability" 0.4/0); הסריקה ב-`gf_filter_benchmark.py` הורחבה ל-72 תצורות One Euro (fc 0.2–1.2 × beta 0–0.02) לצד EMA/Kalman; `step_response_profile` מודד שמונה מדרגות מייצגות (קטנה/בינונית/גדולה × שני כיוונים × שני צירים) והבחירה קוראת את **המקרה הגרוע** ולא מדרגה בודדת; `recorded_transition_ms` מודד זמן מעבר על ההקלטה עצמה, מעוגן בחציוני האות (בלתי תלוי בהיסט כיול) וממדוד מרגע הופעת המטרה כולל חלון ה-STABILIZING; סבילות הדיוק (5%) קובעה כקבוע `ACCURACY_TOLERANCE` **לפני** הניקוד; הדוח מציג שלוש זרועות (ללא סינון / high-stability / אדפטיבי) על אותן דגימות בדיוק, עם mean/median/P90/P95 שגיאה בנפרד מהרעד.

**ברירת המחדל הוחזרה ל-1.2/0.0005** — השינוי הקודם שלי ל-0.4/0 בוטל: ההחלטה על ברירת מחדל נדחית עד שיש מדידה שמצדיקה אותה. בדיקת ה-100ms נשארה assert על ברירת המחדל ולא הוחלשה; נוספו שתי בדיקות נפרדות — אחת מצמידה את יעד ה-100ms ל-preset ה"responsive", והשנייה מתעדת שה-preset הכבד **חורג** מ-100ms בכוונה, כדי שאי אפשר יהיה להפנות אליו את ברירת המחדל ועדיין לטעון שהיעד הושג.

**תוצאה: אף תצורה בכל הסריקה אינה עומדת בשני היעדים יחד.** תצורה אחת בלבד עברה 50% הפחתת רעד (`fc=0.2, beta=0`, ‏58.7%) ובמחיר מדרגה של 1900ms במקרה הגרוע. חזית ההחלפה על round2/TUNE: 100ms→17.7%, ‏150ms→19.6%, ‏200ms→23.8% (ema fc=3), ‏300ms→27.9% (ema fc=2), ‏967ms→34.8% (fc=0.4/beta=0), ‏1900ms→58.7%. ב-round6/T1 בשלוש זרועות על אותן דגימות: רעד P95 380.81 / 159.13 / 350.53px (ללא סינון / כבד / אדפטיבי fc=1.2 beta=0.004), קפיצת פריים P95 147.79 / 25.50 / 82.89px, שגיאה חציונית 209.10 / 196.14 / 196.15px, שגיאה ממוצעת 271.82 / 268.97 / 268.29px, P90 570.41 / 582.83 / 567.32px, כיסוי 100% בכל הזרועות. שים לב: ה-preset הכבד **כן** עובר את יעד ה-50% על T1 (58.2% הפחתה) אך נכשל בתגובתיות; האדפטיבי עובר תגובתיות אך מפחית רעד ב-8% בלבד ב-T1.

**אבחון נתמך-מדידה למה אי אפשר לקבל את שניהם:** שארית התחזית סביב חציון הקיבוע ב-round6/T1 אינה רעש לבן — מתאם עצמי lag-1 ≈ 0.79 בשני הצירים, ועדיין 0.29–0.41 ב-lag 5. מיצוע 32 דגימות (כשנייה) מוריד סטיית תקן מ-87px ל-46px בציר X, בעוד רעש לבן היה נותן 15px. הרכיב הדומיננטי הוא נדידה איטית בפלט המודל המכויל, שמסנן מעביר-נמוך יכול להסיר רק בקבועי זמן של כשנייה. המסקנה: המשך כוונון מסננים אינו המסלול היעיל; המגבלה באיכות האות המכויל.

מדידת המעבר המוקלט התבררה כלא-מבחינה: ~1470ms חציון בכל שלוש הזרועות כולל ללא-סינון, כי חלון ה-STABILIZING וזמן התגובה של המפעיל שולטים בה. היא מדווחת עם המגבלה הזו במפורש ולא כראיה ללג של המסנן.

בדיקות: 311 בדיקות עברו (נוספו 7 — פרופיל מדרגה, בחירה לפי מקרה גרוע, אי-טענת יעד שלא הושג, חסינות מדידת המעבר להיסט קבוע, דילוג על מעבר ללא תנועה). ruff check/format נקיים בשלושת הקבצים שנערכו. dry-run דרך `GazeFollower.process_frame` האמיתי (round98, נמחק אחריו) עבר: 11 פריימים, 31.75 FPS, `finished=true`, אפס שגיאות subscriber, stop_sampling/library_release תקינים, ללא קלט OS. **תאימות לאפיון: לא נמדדה.** השהיית מצלמה-עד-מסך (<50ms באפיון) אינה ניתנת למדידה בחומרה הנוכחית — הספרייה חותמת זמן על פריים בעת קבלתו; זמן המדרגה הסינתטי וזמן המעבר המוקלט אינם תחליף לה. דיוק זוויתי (<1.5° באפיון) לא חושב כאן; הגיאומטריה עדיין NOT VERIFIED.

ביקורת מדידה ואבחון סיבת התזוזה בקיבוע, 7.9.2026: **שתי טענות קודמות שלי נדחות.** (1) "הגברה פי 4.2 על ידי הכיול" — שגוי: הפלט הגולמי דחוס בטווח (פרישת מרכזים 67.7px גולמי מול 320.6px מכויל, יחס 4.7 ≈ ה"הגברה"), ויחס אות-לרעש חציוני דומה (4.94 מול 5.52). (2) "נדידה איטית" — לא הוכח: המתאם העצמי 0.79 חושב על שאריות מאוחדות שנשלטו בידי הקטעים החריגים, וקטעי הקיבוע הם ~1.4 שנ׳ בלבד כך שמיצוע 32 דגימות נשען על בלוק אחד לקטע.

**פגם מדידה שנמצא ותוקן ב-`jitter_metrics`:** הקיבוץ לפי קטע קיבוע היה תקין (בהקלטות אלה לכל מטרה הצגה רציפה אחת בת ~46 דגימות — נבדק), אך ה-P95 חושב על מאגר כל הדגימות ולכן נשלט בידי קטע-שניים. ב-round6/T1: מאוחד 380.8px מול חציון P95 פר-קטע 125.3px (8 מ-10 קטעים סביב 125). כל כוונון המסננים עד כה מוטב מול מספר שנקבע בידי שני קיבועים. נוספו `median_of_fixation_p95_px`, `worst_fixation_p95_px`, `n_fixations` לצד המאוחד, עם בדיקה שמוודאת שקיבוע חריג אחד אינו סוחף את הסיכום הפר-קטע. במדד המתוקן: ללא סינון 125.31px, high-stability 72.30px (‏42.3%-), אדפטיבי 115.54px (‏7.8%-); בקיבוע הגרוע ביותר 698.54 / 253.54 / 699.04px — האדפטיבי אינו עוזר שם כלל.

**הסיבה, נתמכת במדידה חזקה — יציאה מתחום התמיכה של מודל הכיול:** נוסף `support_activation` המודד אקטיבציית ליבת RBF מקסימלית מול 263 וקטורי תמיכה (gamma=0.0005). ב-round6/T1 שני הקטעים היחידים שבהם האקטיבציה מגיעה 0.000 הם בדיוק שני הקטעים שהתפוצצו (535px ו-698px מול ~125px), ובכל השאר המינימום ≥0.49; 2.38% מדגימות T1 מתחת ל-0.01. ב-round2/T2 (הנהון ראש מכוון) **כל** הדגימות באקטיבציה 0.000, ואחד הקטעים קפוא: 531 פריימים בטווח 0.16px — ה-SVR מחזיר את קבוע ההטיה. המנגנון: הרחק מווקטורי התמיכה התחזית נצמדת לקבוע שגוי — היא מפסיקה לעקוב, לא מתרעשת. לכן שום מסנן זמני אינו יכול לתקן את הדגימות האלה. מתאם פיזור פר-קטע עם טווח iod (מרחק/תנועת ראש): r=+0.98 ב-TUNE, ‏+0.56 ב-T1; המודל הנוכחי ללא עמודות ראש ולכן אינו יכול לפצות. מגבלה: n=8–10 קיבועים והמשתנים קולינאריים — אין הפרדה סיבתית מלאה בין תנועת ראש, תזוזת מאפיינים ופתיחות עין.

**נשלל במדידה:** אובדן מעקב (100% SUCCESS ב-round6/T1), חותמות זמן (dt חציוני 32.46ms, אפס dt≤0, אפס פערים >100ms), וחיתוך קואורדינטות (`cm_to_norm` לינארי ללא clamp, נבדק בערכים קיצוניים). סריקת רגולריזציה חוזרת (C∈{1,10,100} × gamma∈{1e-4,5e-4,2e-3} × P∈{0.001,0.1}, אימון על round5/A, ניקוד על round2/TUNE) לא מצאה תצורה שמשפרת מהותית את היציבות: התצורה המשולחת (C=100, gamma=5e-4, P=0.001) כבר מהטובות ליציבות (96.7px), ולכן **לא בוצע refit ולא שונה המודל**.

**לא תוקן בכוונה:** לא הוחלף המודל, לא נוספו עמודות ראש ולא שונו ספי קבלה. הצעד הבא הוא ניסוי תחום שמפריד ראש-יציב מתנועת-ראש באותה מטרה, ולא שינוי ארכיטקטורה. round6/T1 נשאר מסומן כראיית פיתוח שכבר נבדקה.

בדיקות: 312 עברו (נוספה בדיקה שקיבוע חריג אחד אינו שולט בסיכום הפר-קטע); ruff check/format נקיים בשני הקבצים שנערכו. לא נוצר commit. **סקירת Codex לסבב זה חסומה** מאותה סיבה שנרשמה למעלה (מגבלת שימוש, "try again at 6:00 PM"); שינויי `jitter_metrics` ו-`support_activation` ומסקנות האבחון טרם נסקרו באופן עצמאי. **תאימות לאפיון לא נמדדה**: השהיית מצלמה-עד-סמן (<50ms) אינה ניתנת למדידה בחומרה הנוכחית, ודיוק זוויתי (<1.5°) לא חושב — הגיאומטריה עדיין NOT VERIFIED.

round12 נכשל ותוקן פגם בטיחות בהקלטה, 7.9.2026: המשתמש הריץ round12 (T1,T2) אך ההקלטה **בלתי שמישה**: `tracking_state=FACE_MISSING` בכל 903 הפריימים של T1 (וכן ב-T2), `gaze_status` שקרי לאורך כל ההקלטה, `openness=0.0`, `raw_cm` לא-סופי, `feature_dim=0` ואפס שורות collecting. המצלמה עצמה תקינה — נפתחה באותם פרמטרים בדיוק כמו round9 שהצליח (MSMF, ‏640x480, ‏32.26 FPS נמדד) — כלומר הזרם הגיע אך גלאי הפנים לא מצא פנים (עדשה מכוסה, חדר חשוך, המשתמש מחוץ לפריים, או יישום אחר שמחזיק את המצלמה). לא ניתן להסיק דבר מ-round12; ההקלטה נשמרת אך מסומנת פסולה.

**הפגם:** ההקלטה דיווחה `finished=true`, `failure=None`, `watchdog_tripped=false` — כלומר ריצה בת ~6 דקות ללא ולו פריים תקין אחד נראתה כהצלחה. השומרים הקיימים אינם מכסים זאת: ה-watchdog בודק רק הפסקת פריימים, ומונה השגיאות בודק רק חריגות ב-subscriber; פריימים תקינים שבהם אין פנים עוברים את שניהם. זו בדיוק ההפרה של סעיף 4.1 — נתוני מעקב חסרים אינם צריכים להיראות כהצלחה.

**התיקון:** נוסף `MAX_CONSECUTIVE_INVALID_FRAMES=320` (~10 שנ׳ ב-32fps, ארוך בהרבה ממצמוץ או הסחה קצרה) ב-`gf_record.py`; `ProtocolRunner` סופר `valid_gaze_frames`/`consecutive_invalid` ומרים `no_face_stall`, והלולאה הראשית עוצרת בקול עם הודעה שמפרטת מה לבדוק (עדשה, תאורה, מיקום, יישום אחר). `integrity()` מדווח כעת `valid_gaze_frames`, `valid_gaze_fraction` ו-`no_face_stall` תמיד, כך שגם ריצה שלא נעצרה אינה יכולה להסתיר זאת בדיעבד. שלוש בדיקות ייעודיות: ריצה ללא פנים כלל נעצרת; מצמוצים והסחות קצרות (10 לא-תקינים ואז תקין, ×20) אינן מפילות אותה; ו-`integrity` חושף שבר תקינות אפס. סה״כ 315 בדיקות עוברות; השורות שנוספו נקיות ב-ruff (ל-`gf_record.py` יש חוב סגנון קיים שלא נגעתי בו). **סקירת Codex חסומה גם לסבב זה** (אותה מגבלת שימוש). שינוי זה נוגע למסלול ההקלטה החי ומוסיף תנאי עצירה — הוא נבדק ביחידות דרך `ProtocolRunner.on_frame` האמיתי, אך **טרם נבדק מול מצלמה אמיתית ולא נסקר עצמאית**; בפרט הסף 320 פריימים נבחר מנימוק (מצמוץ/הסחה קצרים בהרבה) ולא ממדידה של הסחות אמיתיות.

**ממצא מכריע — הכיול אינו שורד את הסשן, 7.9.2026:** המשתמש דיווח שהנקודה הכחולה קפואה ב-T1 (ראש יציב), ועצר את round13 ב-Esc. הנתונים חד-משמעיים: ב-round13/T1 זוהו פנים ב-100% מהפריימים (`valid_gaze_fraction=1.0`, שומר ה-no-face **לא** נדלק — נכון), אך אקטיבציית התמיכה היא 0.00000 ב-**100%** מהדגימות, והתחזית היא **ערך יחיד** שחוזר על עצמו 231 פעמים: x=0.473065, y=0.070649 — בדיוק אותו קבוע שנמצא קפוא ב-round2/T2. כלומר ה-SVR מחזיר את קבוע ההטיה שלו ואינו ממפה כלל.

מה שהשתנה הוא **מרחב המאפיינים**: המרחק לווקטור התמיכה הקרוב ביותר הוא 220.2 ב-round13, מול 14.7 ב-round6 (אקטיבציה 0.898). גיאומטריית הראש כמעט זהה (iod 0.1532 מול 0.1512–0.1601; eye_mid_x 0.5203 מול 0.5219–0.5242).

**תיקון לטענה שרשמתי קודם:** ייחסתי את הכשל לשינוי תאורה/auto-exposure. **הטענה הזו אינה נתמכת ואני מושך אותה.** המשתמש מסר שלא שינה דבר בתנאי הסביבה, ובדיקת סטיית ה-pitch מהאימון סותרת גם הסבר של תנוחה: round2/TUNE עם סטייה של 8.59° עבד היטב (אקטיבציה 0.909) בעוד round2/T2 עם סטייה קטנה יותר, 4.77°, מת לגמרי (0.000). המתאם בין סטיית pitch לאקטיבציה הוא ‎-0.597 על n=6 — חלש מכדי לבסס סיבתיות. **אין כרגע הסבר מבוסס לכשל של round13.** נשלל במפורש: הפקודה (השוואת מטא־נתונים בין round9 המקורי ל-round13 מראה זהות מלאה במטרות, גיאומטריה, אזור, מסך ונתיב המודל — ההבדל היחיד הוא פרמטרי המסנן, שמשפיעים רק על הציור), ותקינות המודל (ראו למטה).

**המודל תקין — אומת:** `round5/models/today` משחזר על round6/T1 בדיוק את התוצאות הקודמות (חציון 209.3px מול 209.1 שדווח, ממוצע 272.0px). קובצי המודל נטענים, הקונפיגורציה כמצופה (SVR, zscore, C=100, gamma=5e-4, P=1e-3). הכשל אינו במודל.

**אירוע איבוד נתונים — round9 נדרס:** ההקלטה הטובה של round9 (13:41, ‏903 פריימים, אקטיבציה 0.860, ששימשה בהשוואות קודמות) **נדרסה** בהרצה חוזרת עם אותו `--round 9` בשעה 14:34 (session 20260907_143426): 109 פריימים, `aborted=true`, `feature_dim=0`, `valid_gaze_fraction=0.0`. הנתונים המקוריים אינם ניתנים לשחזור; המספרים שדווחו עליהם בעבר נותרים בתוקף כרישום אך אינם ניתנים לאימות חוזר. **הרקורדר לא הגן מפני דריסת סבב קיים — תוקן:** נוסף `guard_existing_round` ב-`gf_record.py` שמסרב להקליט לסבב שכבר מכיל קובצי `.npz`, עם הודעה שמסבירה שהקלטה אינה ניתנת לשחזור ומציעה מספר פנוי או `--overwrite-round` מפורש. אומת מקצה לקצה: הרצת `--round 6` נעצרה עם `refusing to overwrite ... it already holds T1.npz`, ו-round6 נותר שלם (905×258, ‏462 שורות collecting). שלוש בדיקות: סבב עם נתונים נדחה, `--overwrite-round` מתיר, וסבב חדש או תיקייה ללא `.npz` עוברים. סה״כ 318 בדיקות עוברות. **סקירת Codex חסומה גם לשינוי זה** (אותה מגבלת שימוש): השומר נוגע במסלול ההקלטה החי ומוסיף מסלול יציאה חדש (`SystemExit` לפני אתחול הספרייה) — נבדק ביחידות ומקצה לקצה דרך ה-CLI, אך לא נסקר עצמאית.

**דפוס אמיתי — זיהוי הפנים מקרטע:** 14:19 (round12) אפס פנים; 14:27 (round13) 100% פנים; 14:34 (round9 החדש) אפס פנים. הקרטוע לסירוגין, באותה מצלמה ובאותן הגדרות בדיוק (MSMF, ‏640x480, ‏32.26 FPS נמדד בכל המקרים), מצביע על תפיסת מצלמה לסירוגין בידי יישום אחר או על דרייבר שמחזיר פריימים ריקים — לא על תאורה ולא על תנוחה. שומר ה-no-face שנוסף אכן לא נדלק ב-round13 (פנים תקינות) ונדלק כראוי בעקרון במקרים הריקים.

**כלי אבחון חדש ושתי השערות שנשללו, 7.9.2026:** נוסף `gf_camera_probe.py` (OpenCV בלבד; `--face` מוסיף MediaPipe FaceMesh, ‏`--white` מעלה מסך לבן מלא כדי לשחזר את תנאי התאורה של הפרוטוקול). תוצאות אצל המשתמש: המצלמה מספקת תמונה חיה ומוארת (בהירות 145.5, סטיית תקן 40.1, תנועה 1.73), והגלאי מוצא פנים ב-**30/30** פריימים גם על שולחן עבודה רגיל (בהירות פנים 124.1) וגם מול מסך לבן מלא (129.2). לכן **נשללות**: (א) תקלת מצלמה/דרייבר, (ב) ההשערה שהמסך הלבן הגדול שורף את החשיפה והופך את הפנים לצללית — בהירות הפנים אף עלתה מעט מול הלבן.

הכשלים מקרטעים בזמן ולא בהדרגה: 13:41 תקין, 14:19 כשל, 14:27 תקין, 14:34 כשל, 14:39 כשל. round12 (14:19) נכשל **לפני** שינויי הקוד של הסבב הזה, ולכן גם הם נשללים כגורם. נותרה השערה פתוחה שלא נבדקה: מסלול פתיחת המצלמה של הספרייה עצמה (GazeFollower פותחת את ההתקן בעצמה, בנפרד מהבדיקה שלנו, ובלוג הופיע `WebCam closed` שלוש פעמים) — דורש בדיקה בקוד הספרייה אם הכשל יחזור כשהבדיקה עוברת.

**עוד באג שלי שתוקן:** שומר ה-no-face עצר את round14 כראוי אחרי 320 פריימים (~10 שנ׳) במקום לבזבז 6 דקות — אך ההרצה שנעצרה השאירה `A.npz` ריק, ושומר הדריסה חסם אז ניסיון חוזר על אותו מספר סבב. שני השומרים התנגשו. `guard_existing_round` בודק כעת דרך `_holds_usable_rows` את מטא־הנתונים ומתעלם מקבצים ללא `feature_dim` או עם `valid_gaze_frames=0`, כך שקצה שנעצר אינו נועל מספר סבב; מניפסט שאינו קריא נחשב כנתונים אמיתיים כדי שלא תתרחש מחיקה שקטה. אומת: `round14` מותר לניסיון חוזר, `round6` עדיין מוגן. שלוש בדיקות נוספו (קצה ריק אינו חוסם, נתונים אמיתיים לצד קצה כן חוסמים ומזכירים רק את הקובץ האמיתי, מניפסט פגום חוסם). סה״כ 321 בדיקות עוברות; ruff נקי בקבצים שנערכו ובכלי החדש.

**הגורם אותר — הספרייה עצמה מפיקה פלט שבור, 7.9.2026:** הבדיקה המכריעה השוותה, על `raw_cm` (פלט GazeFollower עצמו, **לפני** מודל הכיול שלנו), את השונות בתוך מטרה מול הפרישה בין מטרות. בהקלטה שעוקבת אחרי המבט הפרישה בין מטרות חייבת להיות גדולה מהשונות בתוך מטרה:

| הקלטה | שונות בתוך מטרה | פרישה בין מטרות | מסקנה |
|---|---:|---:|---|
| round5/A | 0.589 | 1.757 | עוקב |
| round6/T1 | 0.574 | 1.934 | עוקב |
| round13/T1 | 0.044 | 0.036 | שבור — קפוא |
| round15/T1 | 1.759 | 0.664 | שבור — רעש |

אותה תמונה במאפיינים ה-258: round6 שונות-בתוך 1.14 מול פרישה-בין 5.26 (תקין); round13 ‏0.0896 מול 0.0849 (וקטור כמעט קבוע לאורך כל ההקלטה, גם בין מטרות שונות); round15 ‏7.62 מול 4.30 (שונות בתוך מטרה גדולה פי 4 מהאימון, ללא מבנה). מרחק לווקטור התמיכה: 14.7 (round6) מול 220.2 (round13) ו-702.6 (round15).

**המסקנה: מודל הכיול אינו האשם ואינו זקוק לחידוש.** הוא מתנהג נכון — SVR שמקבל קלט מחוץ לתחום התמיכה מחזיר את קבוע ההטיה, ומכאן הנקודה הקפואה. השבר במעלה הזרם: MediaPipe מזהה פנים ללא בעיה (30/30 בשתי תצורות תאורה), אך מנוע ה-MNN של הספרייה מייצר embedding שבור — קפוא באחת ההקלטות, רועש באחרת. שני הכשלים "אין פנים" (round12, round14) ו"מאפיינים שבורים" (round13, round15) הם ככל הנראה אותה תקלה בדרגות חומרה שונות, מאז ~14:19. השערה שטרם נבדקה: אתחול/מצב של MNN או תחרות תהליכונים בספרייה; המכונה היא Raptor Lake עם ליבות P/E ו-MNN רץ ב-low precision.

**שורש הנקודה הקפואה אותר ותוקן — הבאג שלנו, 7.9.2026. תיקון לטענה הקודמת:** רשמתי למעלה ש"הספרייה עצמה מפיקה פלט שבור". **הטענה הזו מוגזמת ואני מצמצם אותה.** הגורם לנקודה הקפואה הוא פגם ב-`Standardizer.fit` שלנו: הנרמול מחלק בסטיית התקן שנמדדה ב-35 שניות הכיול, ו-144 מתוך 258 המאפיינים כמעט לא זזו שם (std<1e-3; ‏45 מהם std<1e-6). חלוקה בסטייה זעירה מפוצצת שינוי מאוחר קטן: מאפיין 140 עם std אימון 0.000327 ושינוי מאוחר של 0.28 מקבל z=860, ומוציא לבדו את הדגימה מטווח ליבת ה-RBF. ה-SVR אז מחזיר את קבוע ההטיה — הנקודה נעצרת. פירוק המרחק מאשש: המרחק הכולל ב-round16 הוא 861.6, מתוכו 860.1 ממאפיין בודד; בשימוש בעמודות עם std אימון > 0.01 בלבד המרחק יורד ל-20.3, בטווח הבריא (round6: ‏14.7).

**התיקון:** נוסף `VARIANCE_FLOOR_FRACTION = 0.01` ב-`gf_schema.py` — רצפה לסטיית התקן יחסית לעמודה הרחבה ביותר, כך שאף עמודה אינה יכולה לטעון לפיזור נמוך בהרבה מקנה המידה של מרחב המאפיינים. השומר הקיים טיפל רק בעמודות קבועות מדויקות (אבק נקודה־צפה) ולא בעמודות שכמעט אינן זזות. שלוש בדיקות נוספו (עמודה כמעט-קבועה אינה יכולה לשלוט במרחק; הרצפה יחסית לעמודה הרחבה; עמודות עם פיזור בריא אינן משתנות). 324 בדיקות עוברות; השורות שנוספו נקיות ב-ruff.

**מדידות התיקון:** אימון מחדש על אותם נתוני כיול (round5/A, ללא כיול חדש מול מצלמה) נותן על round6/T1 שגיאה חציונית **209.3 → 174.0px** ואקטיבציה 0.898 → 0.978. בהקלטות שהיו קפואות התחזיות חוזרות לחיים: round16 מ-1/45 ל-45/45 ערכים ייחודיים, round15 מ-20/222 ל-221/222, אקטיבציה מ-0.000 ל-0.77–0.79. המודל המתוקן נשמר ב-`recordings/round5/models/today_floored`. המודל המקורי `today` נשאר כפי שהוא (התיקון חל בעת אימון, לא בעת טעינה) ומשחזר 209.3px כבעבר.

**מה התיקון אינו פותר — נותר פתוח:** גם עם המודל המתוקן, השגיאה בהקלטות מ-14:19 והלאה היא 933px (round15), ‏1086px (round16) ו-1229px (round13), מול 174px ב-round6. כלומר הנקודה כבר אינה נתקעת אך אינה מדויקת, ומשהו בסשנים המאוחרים אכן השתנה מעבר לפגם הנרמול. ההשוואה within/between על `raw_cm` (שאינו עובר את הנרמול שלנו) עדיין מצביעה על הידרדרות ב-round15, ולכן ההשערה על פלט ספרייה מעורער אינה נשללת — אך היא כבר אינה נדרשת כדי להסביר את הקיפאון, ואין לה עדיין ראיה מספקת. **המשימה נותרת בעבודה**; אין לטעון שהבעיה נפתרה.

**המדד שמסביר את הכשלים: אות המבט במאפיינים, 7.9.2026.** הוגדר מדד ישיר — על 88 המאפיינים שנמצאו נושאי-מבט בכיול (מתוך 258; ‏170 שקטים לחלוטין לאורך כל הכיול), היחס בין הפיזור בין מטרות (אות) לפיזור בתוך מטרה (רעש). round5/A: ‏4.46; round6/T1: ‏4.98; round13/T1: ‏0.96; round15/T1: ‏0.60; round17/T1: ‏1.37. כלומר בהקלטות המאוחרות **המאפיינים אינם נושאים מידע על כיוון המבט**, ולכן שום מודל אינו יכול לעבוד מהם — זה מסביר גם למה תיקון הנרמול לא הספיק.

**בדיקת round5 לבקשת המשתמש — הכיול נקי:** 1050 פריימים, ‏100% SUCCESS, בדיוק 45 דגימות לכל אחת מ-9 הנקודות (405 שורות), `finished=true`; מתוך 88 המאפיינים הפעילים, ב-77 האות חזק מהרעש (יחס חציוני 2.46). אין פגם בנתוני הכיול או במודל.

**המתאם היחיד שנמצא — זווית ראש:** pnp_pitch חציוני בהקלטות התקינות: ‏+2.07° (round5/A) ו-‏−0.61° (round6/T1); בכל ההקלטות שאיבדו את האות: ‏−10.50° (round13) ו-‏−11.45° (round15). במקביל `eye_mid_y` ירד מ-0.65–0.66 ל-0.51–0.62. ההפרדה מלאה ללא יוצא דופן, אך **n=5 ואין הוכחת סיבתיות**; זו זווית הנגזרת מנקודות ציון ולא מדידה פיזית ישירה. המשתמש מסר שלא שינה דבר פיזית, ומדדים אחרים אכן יציבים (iod ‏0.151–0.161, פתיחות עיניים 172–237, eye_mid_x ‏0.508–0.530). השערות שנשללו במפורש בסבב זה: תקלת מצלמה/דרייבר, תאורה ומסך לבן, שינוי בקובצי הספרייה (כולם מ-6.9 ללא שינוי), הפקודה, ותקינות המודל.

**כלי חדש:** `gf_camera_probe.py --pose` מציג זווית ראש חיה מול זווית הכיול ומנחה לאיזה כיוון לתקן, כדי לאמת או להפריך את ההשערה תוך חצי דקה בלי לבזבז הקלטה. `--face` ו-`--white` נוספו קודם ושללו את מסלול המצלמה/התאורה.

**האבחון הזה הופרך — ראו התיקון מיד אחריו. הפסקה הבאה נשמרת כרישום של מסקנה שגויה ואין לפעול לפיה.**

**~~האבחון אושש במדידה חיה — המצלמה זזה, 7.9.2026:~~** המשתמש הריץ `--pose` וקיבל **−11°**, בדיוק כפי שנחזה, ומסר שוב שהוא יושב באותה תנוחה. שתי העובדות אינן סותרות: `pnp_pitch` היא זווית הראש **ביחס למצלמה**, ולכן תנוחה קבועה יחד עם זווית נמדדת שהשתנתה ב-11° משמעה שהמצלמה הוסטה. זה מסביר את מלוא התצפיות: הופעה פתאומית ולא הדרגתית, אי-תלות ב-reboot, מצלמה שממשיכה לספק תמונה מצוינת (30/30 פנים) כי מצלמה מוטה אינה מצלמה תקולה, ועליית `eye_mid_y` מ-0.65 ל-0.51 שהיא בדיוק החתימה של הטיית מצלמה. המודל, הכיול ונתוני האימון היו תקינים לכל אורך הדרך; הכיול נעשה בזווית אחת והמדידות המאוחרות הגיעו מזווית אחרת. הפעולה המתקנת אינה בקוד: להחזיר את המצלמה עד ש-`--pose` מציג `MATCHED` ולקבע אותה פיזית.

**התיקון — תיאוריית זווית הראש הופרכה, 7.9.2026:** ב-round17 ה-pnp_pitch **המוקלט** הוא **+2.08°** מול +2.07° בכיול round5 — הפרש של מאית מעלה, כלומר המשתמש ישב בדיוק בזווית הכיול, בדיוק כפי שמסר שוב ושוב. ובאותה הקלטה עצמה אות המבט נותר חלש (1.37) והנקודה קפואה (אקטיבציה 0.000 עם המודל המקורי). **לכן זווית הראש אינה הגורם**, והמתאם על n=5 היה מקרי — round17 הוא הנגד-דוגמה. עדות המשתמש הייתה נכונה בשלוש הפעמים שבהן נמסרה, ואני קיבלתי אותה מאוחר מדי.

**פגם שהוספתי וגרם נזק:** `gf_camera_probe.py --pose` קרא נקודות ציון ישירות מ-MediaPipe, בעוד שההקלטות קוראות אותן מ-`FaceInfo` של GazeFollower. שני המסלולים אינם מסכימים: הכלי הציג ‎−11° בזמן ש-round17 רשם ‎+2.08° לאותה תנוחה. על סמך המספר השגוי הזה ביקשתי מהמשתמש להזיז מצלמה שהייתה מכוונת נכון, והיא אכן הוזזה. המצב פועל הושבת: הפקודה מדפיסה כעת הסבר מלא ומחזירה קוד שגיאה, והמימוש נשמר תחת `_probe_pose_unverified` ללא נתיב קריאה. אין להשתמש בו לכיוון מצלמה עד שתיבדק שקילות קני המידה מול `pnp_deg` המוקלט.

**השלכה מוצרית שנרשמה לטיפול (M2/M3):** מודל מכויל אינו שורד הסטה של ~11° במצלמה, והמשתמש אינו יכול לאבחן זאת — הנקודה פשוט נתקעת או סוטה בלי הסבר. נדרש זיהוי אוטומטי של סטיית גיאומטריה מול תנאי הכיול (למשל השוואת pnp_pitch/eye_mid_y לערכי האימון, או ניטור אקטיבציית התמיכה בזמן אמת) שמתריע "הגיאומטריה השתנתה — נדרש כיול מחדש" במקום להציג מבט שגוי בביטחון. זה נוגע ישירות לסעיף 4.1: אין להציג נתון מבט כאילו הוא תקף כשהקלט מחוץ לתחום הכיול. טרם מומש.

**המודל גובה:** עותק מאומת של `round5/models/today` נשמר ב-`C:\Users\User\Desktop\gazefollower_eval\models_backup\prod-round5-20260907` — ארבעת הקבצים זהים ב-SHA-256, המודל נטען מהעותק ומשחזר את round6/T1 (חציון 209.3px). המיקום מחוץ לריפו הפרויקט, ועם `.gitignore` המכיל `*` כי `C:\Users\User` הוא git root — אומת ש-`git status` אינו רואה אותו. הערה: העותק מכיל וקטורי תמיכה שהם שורות אימון נגזרות-פנים, ולכן הוא נשאר מחוץ למעקב גרסאות; הוא גם **אינו** נמחק על ידי `gf_purge.py`, שמוגבל ל-`gazefollower/recordings` ולקובצי ה-CSV הזמניים — יש למחוק אותו ידנית בסיום הניסוי.

השלכה מיידית: אבחון הרעד אינו יכול להתקדם עד להקלטה תקינה. נדרשת הקלטה חדשה שכוללת A ו-TUNE כדי לאמן מודל טרי מאותו סשן, ורק אז T1/T2 ניתנים לניתוח (הניתוח אופליין על המאפיינים המוקלטים, ולכן אין צורך שהנקודה החיה תעבוד בזמן ההקלטה). round12, round13 ו-round9 שנדרס נשמרים כראיות פסולות ואינם נמחקים. round6 נותר ההקלטה התקינה היחידה מהסשן המקורי.

**נפתר — כיול טרי משחזר תפקוד מלא, 7.9.2026:** הוקלט round18 (A+TUNE+T1, שלושתם `finished=true`, ‏`valid_gaze_fraction=1.0`, ‏32.26 FPS, אפס פריימים אבודים). **אות המבט חזר**: round18/A ‏6.09, ‏TUNE ‏3.49, ‏T1 ‏3.92 — מול 1.37 ב-round17 השבור ו-4.46/4.98 בסשנים המקוריים. כלומר GazeFollower תקינה; מה שנשבר היה התיישנות המודל מול תנאים שהשתנו, לא הספרייה. הטענה הקודמת שלי ש"הספרייה מייצרת embedding שבור" **נדחית** — היא נבעה מהשוואה למודל שיצא מתחום התמיכה שלו.

`gf_fit.py phase0` בחר `svr_fzscore_lnone_C100_g0.0005` על TUNE וניקד את T1 פעם אחת: שגיאה חציונית **175.4px**, אקטיבציה **0.940**, ‏463/463 תחזיות ייחודיות (לא קפוא), כיסוי 100%. **רצפת השונות פועלת**: סטיית התקן המינימלית במודל החדש היא 0.0336 מול 3e-7 במודל round5 — הפגם שהקפיא את הנקודה אינו יכול לחזור.

**אימות על holdout נקי (round19)** — הוקלט אחרי נעילת המודל וההגדרות, לא שימש לאימון או כוונון: 904 פריימים, ‏`valid=1.0`, ‏`finished=true`; אות מבט 2.57; שגיאה חציונית 171.7px; אקטיבציה 0.961; ‏461/461 תחזיות חיות. עם המסנן הכבד (min_cutoff=0.4, beta=0, שננעל מראש ולא כוונן על round18/19): רעד חציון-P95-לקיבוע **264.6→100.1px (‏62%-)**, מאוחד 687.9→197.7px, והשגיאה **משתפרת** 171.7→152.5px.

**מצב היעדים:** יעד הניסוי לרעד (≥50%-) — **הושג** (62% על holdout נקי, הגדרות נעולות מראש). יעד תגובת המדרגה הסינתטית (≤100ms) — **לא הושג**: להגדרה הזו ‏~967ms, והיא נבחרה במודע כהעדפת יציבות על תגובתיות (ראו הבדיקה המעודכנת ל-1000ms). דרישות האפיון — **לא נמדדו**: השהיית מצלמה-עד-סמן (<50ms) אינה ניתנת למדידה בחומרה הנוכחית, והדיוק הזוויתי מדווח כ-ESTIMATED בלבד (חציון 4.80° ב-round18/T1, הרחק מ-1.5°) כי הגיאומטריה עדיין NOT VERIFIED.

**נותר פתוח:** הדחיסה האנכית — שיפוע Y ‏0.740 והיסט 113.5px ב-round18/T1 — שהיא שאלת המחקר המקורית של M2-05 ולא רגרסיה. גם P0-INFO החזיר `no_improvement`: אף תצורה בסריקה לא חילצה מידע אנכי שמיש טוב יותר מברירת המחדל.

**בדיקת התאמת מודל לפני הקלטה, 7.9.2026:** התקלה שעלתה היום — מודל מכויל שיוצא מתחום התמיכה שלו, מחזיר את קבוע ההטיה, והנקודה קופאת בעוד המצלמה, גלאי הפנים וכל שאר השערים מדווחים הצלחה — אינה ניתנת לזיהוי מבחוץ, ועלתה אחר צהריים שלם של הקלטות. נוספה בדיקה מקדימה: `FittedModel.support_activation(X_raw)` ב-`gf_fit.py` (מימוש יחיד; `gf_filter_benchmark.support_activation` הופנה אליו כדי שלא יתקיימו שני חישובים), ו-`preflight_verdict()` ב-`gf_record.py` שמכריע מתוך פריימי החימום שכבר נאספים ונזרקים. סף `PREFLIGHT_MIN_ACTIVATION=0.20` — הרחק מתחת לתחום הבריא הנמדד (0.86–0.98) ומעל הקפוא (0.000), כך שהוא פוסל רק את המקרה החד-משמעי. הבדיקה עוברת רק כשיש `--overlay-model`, מאשרת כברירת מחדל כל מה שאינה יכולה למדוד (מודל ridge, אין פריימים, הכל NaN) כדי שלא תחסום הקלטה על סמך היעדר ראיה, ו-`--skip-model-check` עוקף.

**ממצא חוסם מסקירת Codex, תוקן:** הבדיקה המקדימה אספה את וקטור המאפיינים הגולמי (258 עמודות) בלבד, ולכן מודל שאומן עם עמודות ראש — תצורה שהרקורדר תומך בה במלואה — נכשל על ספירת העמודות (`schema expects 260 columns, caller has 258`) והפיל את הסשן **לפני** ההקלטה. שוחזר בכתיבת מודל head אמיתי מ-round18/A. התיקון: `_overlay_design_row()` מרכיב את השורה בדיוק כפי ש-`ProtocolRunner._predict_overlay` מרכיב אותה, כולל `S.assemble` עם עמודות הראש, ומקבל את אותו `head_builder` שמוזרק לרקורדר במקום לקרוא ל-`H.build` קשיח; פריים שאינו יכול לייצר שורה מדולג ואינו מפיל דבר. בנוסף, קריאת `support_activation` בנתיב החי עטופה ב-try/except שממשיך בהקלטה במקום להפיל אותה — בדיקה שנועדה למנוע בזבוז הקלטה לא תהיה בעצמה סיבה לאבד אחת. אומת מקצה לקצה: מודל head בן 260 עמודות אוסף 50 שורות ברוחב 260 ומחזיר verdict תקין. שלוש בדיקות רגרסיה נוספו (מודל עם עמודות ראש, מודל בלעדיהן, ופריים שאינו ניתן להרכבה).

אימות מול הנתונים האמיתיים של היום, 6 מתוך 6 נכון: round6/round18/round19 (שעבדו) — PASS; round13/round15/round17 (שנתקעו) — REFUSE. שמונה בדיקות יחידה מכסות מודל קפוא, מודל בריא, מיקום הסף מול התחום הנמדד, אי-פסילה על היעדר ראיה, פגיעה חלקית שהחציון עדיין מחזיק, ושלוש בדיקות הרגרסיה של עמודות הראש. סה"כ 332 בדיקות עוברות; ruff נקי בשורות שנוספו; compileall עבר. לא נבדק מול מצלמה חיה — הנתיב עצמו (איסוף פריימי חימום) יאומת בהרצה הבאה עם `--overlay-model`.

**הדחיסה האנכית — נמצא הגורם, 8.9.2026:** עבודה בשני סוכנים במקביל תחת בעלות בלעדית על קבצים (סוכן A: מצב הדגמה ב-`gf_record.py`; סוכן B: חקירה ב-`gf_vertical.py` חדש). אין קובץ משותף; `recordings/` נשאר לקריאה בלבד ואומת כלא-נגוע (`git status` ריק תחתיו, אף mtime לא השתנה).

**הממצא: נרמול המאפיינים הוא הגורם, ולא מאפייני הראש.** בסריקה של 1,872 מועמדים על round18/TUNE בלבד, ההפרדה קטגורית ולא שולית: במשפחת `feature_scaling=zscore` אף מועמד מתוך 864 לא הגיע ל-|1−slope|≤0.1, ובמשפחת `none` הגיעו 523 מתוך 864. הוספת עמודות ראש (`pitch_a`, `eye_mid_y`, `iod_norm`, ‏pnp pitch, וכל head6) שינתה את השיפוע האנכי בפחות מ-0.05 בתוך כל משפחה — **השערת הבלבול עם תנוחת הראש אינה נתמכת**.

**שחזור עצמאי שלי, בלי חיפוש** — שני מודלים באותה תצורה בדיוק (C=100, gamma=5e-4, P=0.001) שנבדלים רק ב-`feature_scaling`, ולכן אינם חשופים ל-winner's curse של דירוג 1,872 מועמדים:

| סט | מודל | x_slope | x_med | y_slope | y_med | y_bias |
|---|---|---:|---:|---:|---:|---:|
| round18/T1 | zscore (בסיס) | 1.087 | 72.9 | 0.717 | 122.3 | 130.9 |
| round18/T1 | f=none | 1.660 | 277.7 | 0.850 | 57.4 | 41.4 |
| round18/T1 | **היברידי x=zscore, y=none** | **1.087** | **72.9** | **0.850** | **57.4** | **41.4** |
| round19/T1 | zscore (בסיס) | 1.097 | 101.1 | 0.654 | 104.2 | 88.5 |
| round19/T1 | **היברידי** | **1.097** | **101.1** | **0.834** | **69.7** | **35.5** |

כלומר כל משפחת נרמול טובה בציר אחר: `zscore` נותן X מדויק ו-Y מכווץ, `none` נותן Y טוב ו-X רחב מדי. מודל לכל ציר לוקח את הטוב משניהם: השגיאה האנכית החציונית יורדת 122.3→57.4px ב-round18 ו-104.2→69.7px ב-round19, ההיסט האנכי יורד בכשני שלישים, **והציר האופקי אינו משתנה כלל**. round19 הוא סשן נפרד שלא שימש לבחירה.

**רצפת השונות שלי זוכתה במפורש:** נבדק אם היא גורמת לדחיסה האנכית. ההפך — עם הרצפה `zscore` נותן y_slope 0.717 ו-x_med 72.9px; בלעדיה (הקוד הישן) y_slope 0.466 ו-x_med 223.0px. היא ממתנת את הבעיה, לא יוצרת אותה.

**מה שלא נפתר:** ההיסט האנכי הקבוע. אף תצורה לא ביטלה אותו, והוא נותר המרכיב הגדול בשגיאה האנכית — חיסור ההיסט היה מוריד את round18/T1 מ-125.4 ל-83.8px, אך זה דורש תוויות בזמן ריצה, כלומר החלטה מוצרית על שלב כיול שני ולא שינוי במתאם. סוכן B דיווח ביושר שהכלל שהגדיר מראש בחר מועמד שנכשל במבחן (y_slope 0.616/0.588, גרוע מהבסיס), ושהזרוע ההיברידית שהצליחה נבחרה post-hoc; ההפרדה הקטגורית בין המשפחות והשחזור העצמאי שלי הם מה שמבסס אותה, לא הבחירה הבודדת.

**מגבלות:** נבדק אופליין בלבד, נבדק אחד, מסך אחד, שני סשנים במרחק דקות. ההיברידי **אינו** ממומש בנתיב המוצר — `gf_record` טוען מודל יחיד, ולכן זו נכון לעכשיו תוצאת מדידה ולא יכולת. לא אומת חי. סוכן B מצא גם שהמדד שמעביר הכללה הוא **השיפוע** (‏Pearson 0.979 מ-TUNE ל-T1) ולא החציון (0.764), בעוד `SELECTION_METRIC` הקיים ממטב את החציון — ממצא post-hoc שדורש אישוש פרוספקטיבי.

**תיקון למספר שדיווחתי:** ציטטתי קודם Y median |dy| ‏113.5px והיסט 113.5px. הערכים הגולמיים ב-`phase0.json` הם היסט **120.5px** (ו-114.5 אחרי סינון); 113.5 הגיע מסיכום משוקלל-מקטעים אחר. הטענה על עצם הדחיסה לא משתנה.

בדיקות ואימות אורקסטרטור: 390 בדיקות עוברות (מ-332; ‏+13 מסוכן A, ‏+45 מסוכן B). בדקתי בעצמי את הדיפים ולא הסתמכתי על הסיכומים: אומת שאין כתיבה לדיסק ב-`run_session` מלבד אחת ממוגנת, ש-`no_save` אינו מוזכר באף שער בטיחות, ושקוד היציאה שקול. **בדיקת מוטציה**: שינוי `if no_save: return None` ל-`if False:` הפיל 3 בדיקות — כלומר הן בעלות שיניים. שוחזרו במלואם המספרים של סוכן B. Codex QA טרם רץ על סבב זה.

**שימור הבסיס ומימוש המודל הדו־צירי, 8.9.2026:**

**1. גיבוי הבסיס המאומת.** המודל וההגדרות של round19 — הגרסה שהמשתמש ראה ואישר — גובו אל `Desktop\gazefollower_eval\models_backup\round19-verified-20260908\` עם `PROVENANCE.txt` המתעד מודל, מסנן, אזור, גיאומטריה והמדדים. אומת זהה בייט-לבייט ומשחזר 171.7px חציון אוקלידי על round19/T1. הגיבוי יושב מחוץ לריפו הפרויקט ועם `.gitignore` משלו (הריפו הביתי `C:\Users\User` לא רואה אותו), ומחוץ לטווח `gf_purge`.

**2. מדד ההצלחה — נמצאה אי־התאמה, לא הוכרעה על דעתי.** ההגדרה המקורית היא **חציון אוקלידי**, לא לכל ציר: `gf_select.py:54` כותב במפורש "The project's own 120 px reference is a **median** target", ו-`RESULTS.md:16` מסמן את 382–508px שמולו נקבע היעד כ**"euclid חציוני"**. מדידה תחת שתי הקריאות (פר-פריים, ללא סינון): round18 בסיס 72.9/122.3/175.3 (נכשל בשתיהן); round18 היברידי 72.9/57.4/**114.8** (עובר בשתיהן); round19 בסיס 101.1/104.2/171.7 (**עובר לכל-ציר, נכשל אוקלידית**); round19 היברידי 101.1/69.7/150.2 (עובר לכל-ציר, נכשל אוקלידית). מעבר למדד לכל-ציר היה מכריז על הבסיס הקיים כהצלחה ב-round19 — החלפת מדד באמצע. המדד לא שונה; שתי הקריאות מדווחות עד להכרעת המשתמש.

**3. מודל דו־צירי מומש.** `--overlay-model-y` ב-`gf_record.py`: מודל שני שמספק **רק** את הציר האנכי, בעוד X נשאר מהמודל הראשי. שניהם רצים על אותו פריים ולכן אינם יכולים להיפרד בזמן. אם המודל האנכי נכשל בפריים — הנקודה **מוסתרת כולה** ולא מוצגת חצי, כי נקודה עם X תקין ו-Y חסר נראית כמו נקודה עובדת שמחליקה על קו אופקי. הבדיקה המקדימה בודקת כעת את **שני** המודלים, כי מודל X טרי עם מודל Y מיושן היה מקפיא ציר אחד בלבד. נוסף מודל `recordings/round18/models/y_axis_fnone` (feature_scaling=none, שאר הפרמטרים זהים).

**פער תיעוד שתוקן אגב כך:** המטא־נתונים תיעדו רק את הגדרות המסנן ולא איזה מודל נטען, כך שקורא מאוחר לא יכול היה לדעת מה המפעיל ראה בפועל. נוספו `overlay_model` ו-`overlay_model_y` למניפסט. הזיהוי של round19 בוצע בסבב זה בהתאמת תחזיות (הפרש 0.000000px מול המודל השמור), לא מרישום.

בדיקות: 394 עוברות (מ-390; ‏+4 למסלול הדו־צירי). **בדיקת מוטציה**: החלפת `return point[0], vertical[1]` ל-`return vertical[0], point[1]` הפילה בדיקה — החלפת צירים שקטה תיתפס. אומת גם מקצה לקצה מול נתוני round19 דרך `ProtocolRunner._predict_overlay` האמיתי: הפלט תואם בדיוק ל-X מהמודל האחד ו-Y מהשני. ruff נקי בשורות שנוספו; compileall עבר. **לא אומת חי** — הנתיב מעולם לא רץ מול מצלמה.

מילסטון: M2 — מבט. מסלול: הערכה של מנוע חלופי, בסביבה נפרדת (C:\Users\User\Desktop\gazefollower_eval, ללא יבוא של gazelink; רישוי GazeFollower CC BY-NC-SA — שאלה פתוחה, ההפרדה אינה פותרת אותה).

מה לעשות (What to do)

לבדוק, בניסוי מבוקר ואופליין, האם הכיווץ האנכי של GazeFollower על המערכת שלנו (שיפוע Y של 0.08–0.38, שגיאה חציונית 382–508px מול יעד 120px) ניתן לתיקון בשכבת הכיול — נרמול מאפיינים ותוויות, פרמטרי SVR — והאם מאפייני ראש מפורשים מוסיפים עמידות לתנועת ראש. בלי לשנות site-packages, בלי קלט אמיתי למערכת ההפעלה, כשהראש חופשי לזוז והסמן עוקב אחרי המבט (לא עכבר־ראש).

שלב 0 (בהיקף הנוכחי): רקורדר שמקליט פר פריים את 258 מאפייני המודל + מאפייני ראש (יחסי landmarks) מאותו פריים, בפרוטוקולים A (כיול 9 נקודות סטנדרטי, נאמן ל־CalibrationController), TUNE (10 מטרות כוונון, seed 20260907), T1 (10 מטרות held-out, seed 20260903), T2 (מבט נעוץ, הנהון מודרך). מתאם אופליין שסורק תצורות על TUNE ומנקד את T1 פעם אחת עם התצורה שנבחרה.

הגדרת סיום (Definition of Done)

1. הקוד בסביבת ההערכה בלבד; בדיקות יחידה (unittest, ללא מצלמה, ללא יבוא gazefollower) עוברות; --dry-run של הרקורדר עובר דרך GazeFollower.process_frame האמיתי עם מצלמה/פנים/מודל מזויפים.
2. הרצת שלב 0 חיה ודוח: טבלת סריקה על TUNE; T1 מנוקד פעם אחת בתצורה הנבחרת; לכל ציר — חציון |שגיאה|, היסט, שיפוע, יציאות מהמסך; לפני ואחרי החלקה; FPS; שיעור אובדן; מספר שורות שנפלו בסינון.
3. שערי החלטה מדווחים במספרים: G1 (מידע אנכי: שיפוע Y על T1 בתצורה הנבחרת, לצד שגיאה והיסט), G2 (אבחון: |corr(pitch_a, target_y)| על שורות A), G3 (תקפות הקלטה: head_valid<2% נכשל, 45 שורות לנקודה, FPS≥25, טווח pitch ב־T2).
4. כישלון G1 אינו מוכיח "אין מידע במודל"; המסקנה המותרת: "לא נמצאה תצורה שמחלצת מידע אנכי שמיש עם המאפיינים והמתאם האלה". ההמשך — החלטת המשתמש.
5. הקלטות (embedding ביומטרי־נגזר) רק תחת recordings/ עם .gitignore, ללא פריימים/תמונות/landmarks, לא מועתקות לריפו, לא נשלחות ל־Codex, נמחקות ב־gf_purge.py בסיום.
6. סקירות Codex: תוכנית לפני מימוש; יחידת עבודה אחרי הקוד; סופית לפני סימון. תוצאות נרשמות כאן.
7. ניסוי הראש עצמו (A/B_neutral/B/C, שלושה סבבים, G4 כולל מסלול הצלחה של עמידות לתנועת ראש בתנאי שדיוק ראש־יציב נשמר) — מחוץ להיקף הנוכחי; ייפתח רק אחרי תוצאות שלב 0 והחלטת המשתמש.

מצב: בעבודה (7.9.2026). שלב 0 נכתב, תוקן לפי סקירה, ועבר בדיקה אוטומטית. טרם רץ מול מצלמה — אין עדיין שום מספר דיוק.

מיפוי מול מסמך היעדים (7.9.2026)

אבני דרך:

M0 — חיבור למימוש ומדידה: IN PROGRESS. הרקורדר, סכמת ההקלטה, המתאם, מניפסט ההתקנה, שני סטי המטרות, חשבונאות הזמינות, האחוזונים, השגיאה הזוויתית המקורבת ומטריצות האזור קיימים ועברו בדיקה אוטומטית. חסר: הרצה חיה.

M1 — בדיקת שילוב ראש: IMPLEMENTED — NOT VALIDATED. תשתית הזרועות קיימת; קריטריוני R ומסכת זכאות משותפת ל־B/C טרם נכתבו.

M2 — דיוק בטווח התנועה: TODO. 13 תנוחות סטטיות, 6 משפחות תנועה ו־4 שילובים אינם ממומשים.

M3 — כיול, זמן ושימוש רציף: TODO.

M4 — שלושה סשנים והחלטה: TODO.

יעדים — כולם NOT MEASURED מול חומרה. הסטטוס שהמערכת מדפיסה בסיום כל ניתוח:

G1 דיוק במעלות: NOT VERIFIED. הזווית מחושבת ממיקום עיניים מוערך — המרחק מוצהר על ידי המפעיל, וההטיה של המצלמה כלפי מעלה אינה ממודלת. קיים הליך אימות (validate_scale) ופרמטר camera-pitch-deg שיהפכו אותה למאומתת.

G2 P90 במעלות: NOT VERIFIED. נשען על אותה גיאומטריה.

G3 טווח ראש: NOT MEASURED.

G4 זמינות: נמדד בתנאי ראש יציב בלבד. הכיסוי מחושב על כל השורות הזכאיות.

G5 קצב: PARTIAL. קצב חציוני נרשם; קריטריון חלונות 5 השניות לא ממומש.

G6 השהיה: NOT MEASURED. הספרייה חותמת זמן על פריים בעת קבלתו ולא בעת החשיפה, ולכן מדידה מקצה לקצה אינה אפשרית בחומרה הנוכחית. תועדה שיטה חלופית.

G7 זמן כיול: PARTIAL. G8, G9, G10, G11: NOT MEASURED.

הכרעות R / H / GO: NOT MEASURED.

עבר בדיקה אוטומטית, לא נבדק מול מצלמה:

172 בדיקות יחידה עוברות. אף בדיקה אינה מייבאת gazefollower, פותחת מצלמה או יוצרת חלון.

קבצים בסביבת ההערכה: gf_common, gf_head_features, gf_schema, gf_fit, gf_record, gf_purge, ובשלב זה נוספו gf_setup, gf_targets, gf_geometry, gf_report. תשע חבילות בדיקה.

שבעה ממצאים חוסמים מסקירת Codex תוקנו, ולכל אחד נכתבה בדיקת רגרסיה שמשחזרת את התרחיש שדווח:

1. אובדן מעקב נעלם מהמכנה. rows_collecting דרש gaze_status, ולכן פריים שאבד לא הגיע להערכה כלל: 160 שורות איסוף עם 80 אבודות דיווחו n_rows=80 ו־n_lost=0. נוסף rows_eligible שאינו מותנה במעקב, וההערכה רצה עליו. זה בדיוק מה ש־G4 של המסמך דורש.
2. שער התקפות אישר הקלטה חלקית. נספרו רק מטרות שנוכחו ו־aborted/finished לא נבדקו: מטא של 9 מטרות עם מטרה אחת בלבד ו־aborted=True יצא תקף. עכשיו נבדקות המטרות הצפויות, ובדיקה שלא ניתן להעריך נחשבת לא־תקפה ולא לתקפה.
3. אחסון ה־embedding לא היה כלוא. --out קיבל יעד שרירותי; עכשיו נאכף בקוד שהכתיבה תהיה תחת recordings בלבד.
4. ה־timeout של הניקוי נעקף. close ו־release של הספרייה מבצעים join ללא הגבלה; כל שלב ניקוי עטוף עכשיו ב־timeout נפרד, כך שתקיעה אינה מונעת שחרור המצלמה וסגירת הקובץ.
5. חריגות subscriber חוזרות השאירו הקלטה רצה בלי לשמור כלום. נוסף abort אחרי עשר חריגות רצופות, עם הצגת השגיאה על המסך.
6. freeze התחרה ב־subscriber באוויר. עכשיו יש השהיית ניקוז ונעילה.
7. שורת "אחרי החלקה" הציגה שיפוע בלי היסט — סתירה ישירה לתיקון שאושר. כל שורת דיווח כוללת שגיאה, היסט ושיפוע.

תוקנו גם: בחירה שדוחה מדדים לא־סופיים, חיזוק execute_purge, ומקרה קצה שבו 45 ערכים זהים של 0.1 נתנו סטיית תקן של כ־4e-17 ולכן עמודה קבועה נורמלה לערכים של 1 במקום 0.

התנגשות שמות נפתרה: שערי שלב 0 נקראים P0-INFO, P0-CONFOUND ו־P0-VALID. השמות G1 עד G11 שמורים ליעדי המסמך בלבד.

התנגשות רשת המטרות אותרה במספרים ותועדה: רשת 16 המטרות של המסמך מול רשת הכיול של הספרייה נותנת 8 מתוך 16 מתחת לסף 250 פיקסלים של הפרויקט, והפינות רק 98. לפי החלטת המשתמש שני הסטים מוקלטים בכל סשן: GRID16 כמפורט במסמך עם מרחק לנקודת הכיול הקרובה לכל מטרה, ו־T1 המוחזק (0 מתוך 10 קרובות, מינימום 252) כמדד ההכללה. הדוח מפריד ביניהם ואינו ממזג.

dry-run דרך process_frame האמיתי, עם מצלמה, זיהוי פנים ומודל מזויפים, על כל חמשת הפרוטוקולים: זרוע A נתנה בדיוק 405 שורות מתקבלות, אפס שגיאות, ניקוי נקי, והקלטות נכתבו ל־recordings בלבד. הצנרת האופליינית רצה עליהן והפיקה דוח מלא. עם מודל רעש אקראי דווח נכון "ללא שיפור" ו־R בריבוע שלילי — בקרה שלילית.

gf_purge נבדק בפועל: מחק 12 קבצים ואת קובץ ה־CSV של הסשן שלנו בלבד, והשאיר את .gitignore.

לא נבדק כלל:

כל תוצאות שלב 0. דורש הרצה מול מצלמה.

FPS, השהיה, שיעור head_valid ודיוק אמיתיים.

האם הרקע הלבן משנה את התוצאה מול ארבע ההרצות הקודמות, שנמדדו על רקע כהה בעוד הכיול של הספרייה תמיד היה לבן. ההרצות החדשות אינן בנות־השוואה ישירה לישנות.

סקירת Codex: תוכנית — בוצעה. יחידת עבודה — בוצעה, שבעה ממצאים חוסמים תוקנו כמפורט למעלה.

ראיות מדידה חיה — 7 בספטמבר 2026 (מאוחר יותר באותו יום)

שתי הרצות חיות מלאות בוצעו על ידי המשתמש (round0, round1): כל עשר ההקלטות תקפות — 32.3 FPS, 100% כיסוי, אפס אובדן, אפס שגיאות. הדוח המלא: gazefollower/RESULTS.md. הכלי: gazefollower/gf_diagnose_bias.py.

התוצאה: הכיווץ האנכי נפתר. שיפוע Y עלה מ־0.08–0.38 בהרצות הישנות ל־0.71–0.86, וחציון שגיאת Y ירד ל־87–91 פיקסלים — מתחת לסף 120 של הפרויקט, בשתי ההרצות. הסיבה: רקע אחיד בכיול ובמדידה (הישנות כיילו על לבן ומדדו על כהה), והמידע האנכי נמצא ב־256 ממדי ה־embedding ולא בפלט הדו־ממדי הגולמי — שם המתאם עם המטרה הוא אפס.

הממצא החדש: הציר האופקי נכשל, וזו מגבלת מנוע ולא מגבלת כיול. הפלט הגולמי של המודל ב־X רווי בקצוות: על רשת 16 המטרות הוא אינו מונוטוני, אינו מבחין בין x=0.05 ל־0.35 ולא בין 0.65 ל־0.95, וכל ההבחנה האופקית נמצאת בצעד המרכזי היחיד (יחס צעדים 0.13 ב־round1, 0.32 ב־round0). הטווח הגולמי ב־X דחוס פי 17–27 מול התוויות, לעומת פי 3–4 ב־Y. מתוך 40 תצורות כיול, אף אחת לא מביאה את X מתחת ל־120; הטובה ביותר (208px) הורסת את Y. השגיאה ב־X אינה היסט קבוע אלא סקאלה לא יציבה שסימנה מתהפך בתוך סשן.

תנוחת הראש נפסלה כסיבה במספרים: ב־round1 הראש היה נעול (הזזת עיניים בין כיול לבדיקה ≤4.5px, yaw IQR 3.7°) ו־X נכשל בכל זאת. תוקנה הסקה שגויה קודמת שהציגה מתאם 0.98 כתזוזת ראש — מתאם הוא חסר־קנה־מידה, והמשתמש צדק שכמעט לא הזיז את הראש.

השערה (לא הוכחה): המסך ברוחב 120 ס״מ במרחק 60 ס״מ הוא ±45°, מעבר לטווח האימון הסביר של המודל. הבדיקה שתכריע: סשן עם מטרות באזור המרכזי בלבד (x בין 0.3 ל־0.7).

מה שלא נמדד: מעלות (G1/G2 — NOT VERIFIED, מיקום עיניים מוערך); השהיה (G6 — NOT MEASURED); G3, G8–G11 מחוץ להיקף; T2 הוקלט ותקף אך טרם נותח. ההרצות אינן בנות־השוואה ישירה לארבע הישנות.

הכרעה לפי מסמך היעדים: R/H/GO — NOT MEASURED. מצב מומלץ: PARTIAL, "רק טווח קטן יותר עובר". ניסוי מאפייני הראש (M1) אינו מומלץ להמשך: הראש נפסל כסיבה, ומאפייני ראש אינם מוסיפים אות שהמודל אינו מפיק.

תיקוני רגרסיה בסבב זה, כולם מסקירות Codex בשער העצירה: timeout שחסם לולאה ולא קריאה חוסמת; probe שנכשל ונפל דרך ההגנה להקלטה; המתנה אינסופית שצברה embeddings ללא הגבלה; watchdog משותף שהפיל פרוטוקול אחרי הפסקת קריאה. לכל אחד בדיקת רגרסיה. 205 בדיקות עוברות. הקוד הועבר לריפו תחת gazefollower/ לבקשת המשתמש; הספרייה עצמה נשארת ב־venv חיצוני.

סקירה סופית — QA BLOCKED (7.9.2026). שתי משימות סקירה של שער העצירה נתקעו: אחת 54 דקות בשלב starting והשנייה 17 דקות בשלב verifying, ואחת מהן החזירה JSON לא תקין. שתיהן בוטלו. לפי סעיף 7.5 בהוראות הפרויקט, סקירה שנכשלת או פוקעת נרשמת כחסומה ולא כעוברת, ולכן המשימה אינה יכולה להיות מסומנת DONE על סמך המצב הנוכחי. התיקונים עצמם נשענים על סקירת יחידת העבודה שכן הושלמה, ועל 172 בדיקות אוטומטיות.

הערה תפעולית לסבב הבא: פקודת הביטול של התוסף נכשלת כשמריצים אותה מ־Git Bash, מפני ש־MSYS ממיר את הארגומנט /PID לנתיב. העקיפה היא MSYS_NO_PATHCONV=1 לפני הפקודה, או הרצה מ־PowerShell באותה ספריית עבודה.

12. סקירת התאמה של eye-gaze-system / Eyecom — 7.9.2026

בקשת המשתמש: בדיקת עומק של הקוד ב־C:\Users\User\Documents\eye-gaze-system מול GAZELINK כפי שהיא כעת, וזיהוי דברים שאפשר להטמיע. זו סקירה ללא מזהה מימוש חדש; אין שינוי ב־What to do, ב־Definition of Done או בהשלמת אבני הדרך.

מצב: סקירת הקוד והבדיקות המבודדות הושלמה. לא בוצעה הטמעה ולא נבדקו מצלמה, משתמש, קלט Windows או ה־service.exe המצורף. סביבת GazeFollower החיצונית של M2-05 לא נסקרה; התיעוד שלה נקרא כדי לא לבלבל בין המסלולים.

תוצר: [דוח ההשוואה המלא](docs/eyecom-comparison-2026-09-07.md), עם ממצאים והפניות לקוד, מפת אימוץ לפי המשימות הקיימות, מגבלות ותנאי אימות מוצעים. קשור בעיקר ל־M2-04, M3-01/02 ו־M4-01/03, ובוחן גם את API/אריזה של M5/M6.

ממצאים שאומתו: גלאי Eyecom מזהה עיגול שחור מלאכותי ללא עין כ־PUPIL_STABLE וביטחון 1.0; פונקציית דגימת הכיול מקבלת חוצץ ישן גם ב־NO_EYE וביטחון אפס; Start אינו בודק כיול; reset אינו מנקה את פריים ה־preview. מיקום הסמן אינו משתמש בנתוני הכיול, והגדרות blink/dwell נשמרות בענן בלי מסלול ביצוע במנוע. ההמלצה היא לאמץ רעיונות של ממשק מצב, פרופיל, בדיקת תקיעת מצלמה והגבלת מהירות סמן, תוך מימוש בחוזי GAZELINK ובסדר המשימות הקיים. אלו הצעות ולא החלטת מוצר חדשה.

בדיקות: pytest — 532 passed, 2 deselected, 9.88s, כיסוי 84%; ruff check עבר; ruff format --check עבר (104 קבצים); mypy עבר (87 קבצים). ברפרנס: שבעה קובצי Python עברו AST parse, ארבע בדיקות התנהגות מבודדות שיחזרו את הממצאים, npm lint נכשל עם 6 שגיאות ואזהרה, ו־npm build עבר אחרי חסימת spawn EPERM בהרצה המוגבלת והרצה מאושרת. ה־build יצר dist מקומי שאינו במעקב; לא הופעלו Electron או שירות עכבר.

סקירה עצמאית: Sol בדק מנוע ובטיחות; Terra בדק UI/API/אריזה, ללא עריכות. הסקירה המרכזית קראה את מסלולי הקוד עצמם ואימתה את הבדיקות. הדוח מבחין בין חוזי בטיחות קיימים לבין שליטה עתידית שטרם מומשה; מספר בדיקות וכיסוי אינם מדדי דיוק מבט.

QA סופי של הדוח: Sol ביקש לדייק ש־36 נקודות כיול מתייחסות למסך המדידה בלבד, ולהוסיף M6-01 לרשימת המיפוי. הסקירה המרכזית אימתה את הקריאות במתאם ובשני החלונות ותיקנה את הדוח; יתר הממצאים אושרו מבחינת מנוע ובטיחות. פער 26/36 נשאר ממצא למעקב, ללא שינוי קוד.

קבצים ששונו בסבב: docs/eyecom-comparison-2026-09-07.md ו־TASKS.md בלבד. שינויים מוקדמים של המשתמש ב־TASKS.md ומחיקת txttt.txt נשמרו. מאגר הרפרנס ללא שינויי מקור במעקב. לא נוצר commit. האפיון הישן לא עודכן למרות פערי תיאור המצב שנמצאו, ולא שונתה תוכנית הניסוי הפעילה.

עדכון הפעלה לבקשת המשתמש, 7.9.2026: לאחר הסקירה הופעלה אפליקציית Eyecom דרך Electron עם ה־dist שנבנה וה־service.exe המצורף. שני עותקי ה־EXE ברפרנס זהים לפי SHA-256; התאמת הבינארי למקור עדיין אינה מאומתת. שירות /status החזיר HTTP תקין, ובבדיקת המצב לאחר ההפעלה דיווח TRACKING / NO_EYE, confidence=0, stable_frames=0. הסוכן לא שלח /start או /stop; בקשות אלו הופיעו בלוג במהלך ההפעלה האינטראקטיבית. אומתה עליית התהליך והשירות בלבד; לא אומתו הגעת סמן למטרות, דיוק או שליטה שימושית. קוד המוצר לא שונה והאפליקציה הושארה פתוחה לבדיקה בידי המשתמש.


13. ניסוי כיול מחדש — האם כיול חוזר מחזיר את הדיוק? — 8.9.2026

השאלה, כפי שאושרה על ידי המשתמש: הדיוק הידרדר במהלך הסשן של 8.9.2026. הרצות 5–9 דקות אחרי הכיול של round18/A מדדו 171.7 ו־151.9 פיקסלים חציון אוקלידי, והרצות 78–125 דקות אחר כך מדדו 158.1 עד 331.5. השאלה התפעולית הצרה: האם כיול מחדש מחזיר את הדיוק. אין כאן מזהה משימה חדש; זו עבודת מדידה תחת M2 שנועדה להכריע אם GAZELINK חייב כיול מחדש לפי לוח זמנים.

מה בוצע בפועל

הוקלט round27 (A, TUNE, T1) באותה ישיבה, אותה תאורה ומצלמה שלא הוזזה: A — 405 שורות מתקבלות, TUNE — 368, T1 — 463, 32.26 FPS, אפס שגיאות, 0% head-invalid, כיבוי נקי. הותאם `gf_fit.py phase0` על round27 (42 מודלים נשמרו; T1 נוקד פעם אחת ולא נכנס לאימון או לבחירה). הסריקה בחרה `svr_fzscore_lzscore_C100_g0.0005` (125px על TUNE), אך לפי התוכנית **הוצמדה תצורת round18** — `svr_fzscore_lnone_C100_g0.0005` — כדי שהניסוי יבדוק כיול ולא יחליף תצורת מודל באותה נשימה. תצורת הסריקה הזוכה נשארת ניסוי נפרד שטרם בוצע. הוקלט round28 (T1 בלבד, סדר קבוע, אותו מסנן one-euro fc=0.4/beta=0) עם המודל החדש כ-overlay: 458 שורות איסוף, 32.26 FPS, אפס שגיאות, בדיקת המודל בטעינה 0.941 אקטיבציה.

התשובה: כיול מחדש לא החזיר את הדיוק

שלוש התוצאות מדווחות בנפרד, כפי שנדרש, ולא־מסונן מופרד ממסונן:

1. האם המודל החדש טוב מהישן על אותם פריימים? **תלוי בהקלטה, וזו עצמה תוצאה.** על round28/T1 זו תיקו — חציון הפרש מזווג 3.9px ללא סינון (המודל החדש טוב יותר ב-52% מהפריימים) ו-5.9px עם סינון (54%). על round27/T1 המודל החדש טוב בהרבה — 175.2px ללא סינון (89% מהפריימים) ו-186.6px עם סינון (100%). ההבדל אינו במודל החדש (175.2 מול 191.8 מסונן בשתי ההקלטות) אלא במודל הישן, שנתן 386.9 על round27/T1 ו-194.1 על round28/T1 — שתי הקלטות בהפרש שלוש דקות באותם תנאים מוצהרים. תנודה של כ-190 פיקסלים בין שתי מדידות מוחזקות סמוכות היא ממצא בפני עצמו, והיא אינה זמן שחלף ואינה המודל.

2. האם חזרנו לרצועת 150–170 פיקסלים? **לא.** round28 מדד 201.2 לא־מסונן ו-191.8 מסונן, 2.6 דקות אחרי הכיול. round27/T1 המוחזק מדד 192.5 ו-175.2. שניהם גרועים מ-round19 (171.7 לא־מסונן, 5.0 דקות) ומ-round20 (151.9, 8.6 דקות), וגרועים מ-round22 שנמדד 158.1 שמונים דקות אחרי הכיול.

3. האם יעד 120 הפיקסלים הושג? **לא**, ולא בקרבת מקום, בשום זרוע.

למה: הכיול החדש עצמו חלש מהישן

באותה תצורה מוצמדת ואותו קוד התאמה, חציון TUNE הוא 136.9px ב-round18 מול 163.5px ב-round27. ה-T1 המוחזק של כל סשן, מנוקד במודל של אותו סשן: round18/T1 — 175.3 לא־מסונן ו-167.3 מסונן; round27/T1 — 192.5 ו-175.2. כלומר הכיול של הבוקר היה טוב יותר מהכיול שנרשם עכשיו. זה מסביר את התוצאה בלי להיזקק להזדקנות המודל.

טבלת הזמן מאז הכיול, כעת n=9 ומנוקדת דרך אותו מסלול קוד (מסונן = one-euro fc=0.4/beta=0):

| הרצה | כיול | דקות | לא־מסונן | מסונן | med abs dx | med abs dy | אקטיבציה |
|---|---|---|---|---|---|---|---|
| round19 | round18 | 5.0 | 171.7 | 152.4 | 68.2 | 65.1 | 0.961 |
| round20 | round18 | 8.6 | 151.9 | 134.6 | 62.3 | 99.1 | 0.916 |
| round21 | round18 | 78.0 | 262.6 | 242.3 | 155.1 | 141.6 | 0.982 |
| round22 | round18 | 79.9 | 158.1 | 173.5 | 75.1 | 68.0 | 0.971 |
| round24 | round18 | 82.2 | 269.4 | 233.7 | 120.1 | 153.0 | 0.922 |
| round25 | round18 | 122.4 | 240.6 | 233.9 | 110.6 | 153.7 | 0.958 |
| round26 | round18 | 124.5 | 331.5 | 314.8 | 157.2 | 249.2 | 0.936 |
| round27 | round27 | 0.0 | 192.5 | 175.2 | 113.0 | 111.6 | 0.983 |
| round28 | round27 | 2.6 | 201.2 | 191.8 | 184.1 | 48.8 | 0.984 |

המתאם בין זמן שחלף לשגיאה נשאר חיובי בתוך הסשן הישן (r=+0.855 על שבע ההרצות עם הכיול של round18, מסונן), אך שתי הנקודות החדשות אינן ממשיכות אותו: כיול טרי בן 2.6 דקות מדד גרוע יותר מכיול בן שמונים דקות. **השערת הזמן אינה מאוששת.** היא גם לא נשללה — כיול חלש יותר יכול להסתיר אפקט זמן — אך היא כבר אינה ההסבר החסכוני.

אקטיבציית התמיכה גבוהה בכל ההרצות (0.916–0.984 חציון, 0% מהפריימים מתחת ל-0.01), כולל בהרצות הגרועות. כלומר המודל מכסה את הקלט ופשוט פחות מדויק; זה אינו מקרה של יציאה מטווח הכיול.

הערה על המסנן: ההנחה שהמסנן שווה 20–40 פיקסלים אינה מדויקת. במדידה כאן הוא שווה 6.7 עד 35.7 פיקסלים, וב-round22 הוא **הרע** את החציון ב-15.4 פיקסלים. לכן שתי הזרועות מדווחות תמיד בנפרד.

תיקוני קוד בסבב

`gf_filter_benchmark.py`: נוספה `_presentation_windows` המפצלת לפי רצפים רציפים של target_id. `jitter_metrics` קיבצה לפי מזהה מטרה, ולכן עם `--repeat-first` מיזגה שתי הצגות במרווח כחצי דקה ל"קיבוע" אחד והציגה את הסחיפה ביניהן כרעד. `recorded_transition_ms` סבלה מאותה תקלה בצורה חמורה יותר: המילונים `steady`/`search` היו ממופתחים לפי מזהה מטרה, ולכן שרד רק הביקור האחרון והמעבר הראשון אל אותה מטרה תוזמן מול עוגן שנרשם חצי דקה מאוחר יותר; עכשיו ההצגות ממופתחות לפי מיקום וחלון ה-onset נמצא לפי הכלה. מדידה בפועל: round25 עבר מ-10 ל-12 הצגות וחציון-P95 של קיבוע ירד מ-291.9 ל-233.6 פיקסלים, round26 מ-184.1 ל-156.9. על הקלטות ללא חזרות (round18) המספר לא השתנה — התיקון אינו משנה אף מדידה קודמת.

`tests/test_filter_benchmark.py`: ארבע בדיקות בתחתית הקובץ היו מוזחות **בתוך** הפונקציה `_tune_row` אחרי ה-return שלה, ולכן unittest מעולם לא אסף אותן. `_tune_row` הועברה לסוף המודול והבדיקות קיבלו מחלקה אמיתית (`PresetGuardTests`); שלושתן עוברות. נוספו שלוש בדיקות לתיקון ההצגות (מטרה חוזרת היא שני קיבועים; אין קפיצת פריים מומצאת על פני הפער; הביקור השני מתוזמן מול העוגן שלו).

`gf_recal_compare.py` (חדש): מנקד הקלטה אחת בשני מודלים על אותן שורות בדיוק — אותה תנועת עין, אותו רגע, אותה תנוחת ראש, אותו מסנן — כדי להוציא את רעש ההרצה שהפך כל השוואה קודמת בין הרצות לבלתי קריאה. קריאה בלבד: אין מצלמה, אין קלט OS, אין התאמת מודל מחדש. `tests/test_recal_compare.py` (חדש): ארבע בדיקות, כולל שמירה על סימן ההפרש (ישן פחות חדש) כדי שרגרסיה לא תדווח כשיפור, ושורה שרק מודל אחד חזה אינה נכנסת לזיווג.

קבצים ששונו: `gazefollower/gf_filter_benchmark.py`, `gazefollower/tests/test_filter_benchmark.py`, `gazefollower/gf_recal_compare.py` (חדש), `gazefollower/tests/test_recal_compare.py` (חדש), `TASKS.md`. נוצרו `recordings/round27`, `recordings/round28`, `results/round27`, `results/recal/`. לא נוצר commit.

בדיקות שהורצו

סוויטת gazefollower: 411 עברו, 2 נכשלו — שתי בדיקות ב-`test_m0.py` שנכשלות רק כי `screeninfo` חסר ב-`.venv` של הפרויקט; תחת סביבת ההערכה שיש בה את החבילה, `ManifestTests` עוברת במלואה (10/10). כשל תלוי־סביבה, לא קשור לשינוי. ruff check ו-ruff format עברו על ארבעת הקבצים. mypy של הפרויקט אינו מכסה את `gazefollower/` (files = src, tests, analyze.py, export_test_targets.py); הרצה כפויה עליו מחזירה גם על גרסת ה-HEAD 21 שגיאות באותו קובץ, כלומר בסיס קיים ולא רגרסיה. `gf_record.py --list-monitors` אימת 5120x1440 לפני ההקלטות.

Codex QA — חסום

נשלחה בקשת סקירה קריאה־בלבד ממוקדת לשני קבצי יחידת העבודה (לא לעץ העבודה כולו, שמכיל שינויים לא־קשורים). Codex החזיר "You've hit your usage limit... try again at 2:01 PM". גם שער העצירה האוטומטי נכשל בסוף הסבב (task-mtsf709q-dfptii, status=failed, שש שניות, פלט ריק) — אותה סיבה בדיוק, לא תקלת קוד ולא תקלת פלאגין. לפי סעיף 7.5 זה נרשם כ-QA חסום ולא כעובר; השער לא הושבת ולא בוצע ניסיון עוקף. יש לחזור על הסקירה אחרי חידוש המכסה. כל הבדיקות לעיל הן ולידציה עצמית ואינן תחליף.

מה נשאר פתוח

התנודה של כ-190 פיקסלים במודל הישן בין round27/T1 ל-round28/T1, שלוש דקות זו מזו. שתי ההקלטות נבדלות בכך של-round28 הייתה נקודה כחולה חיה על המסך ול-round27/T1 לא, ובכך שמדדי הראש זזו (roll 5.9° מול 8.4°, eye_mid_y 0.446 מול 0.456). לא נקבע מי מהם אחראי, ואי אפשר להסיק זאת משתי הקלטות. עד שזה מובן, גם השוואות בין הרצות בהפרש דקות אינן אמינות.

למה הכיול של round27 יצא חלש מזה של round18 — לא ידוע; לא נמדד מה השתנה בתנאי הכיול עצמו.

האם קיים רכיב מרחבי ב-X בנפרד מהסחיפה. קצב הדעיכה (+0/+30/+60 דקות ללא נגיעה) — התוכנית התנתה אותו בכך שהכיול מחדש יעבוד, והוא לא עבד, ולכן אינו מומלץ כעת. תצורת הסריקה הזוכה של round27 (`svr_fzscore_lzscore_C100_g0.0005`, 125px על TUNE מול 163.5 של המוצמדת) — ניסוי נפרד, טרם בוצע, וכעת המועמד הסביר יותר להמשך מאשר עוד כיול. יעדי האפיון (1.5° דיוק, 50ms השהיה מצלמה־לסמן) נותרו בלתי נמדדים בחומרה הזו. הכול נבדק על נבדק אחד ומערכת אחת.

בדיקת המודל המועמד וזיהוי הגורם לפער — 8.9.2026 (המשך לסעיף 13)

לבקשת המשתמש נבדק המועמד שכבר אומן, בלי הקלטה נוספת: `svr_fzscore_lzscore_C100_g0.0005` מ־round27, שזכה בסריקה עם 125px על TUNE מול 163.5px של התצורה המוצמדת.

המועמד נדחה. הוא נוקד על שתי הקלטות המבחן מול שני המודלים שכבר נמדדו, על אותן שורות בדיוק:

| מודל | round27/T1 מסונן | round28/T1 מסונן |
|---|---|---|
| חדש (round27, תצורה מוצמדת) | 175.2 | 191.8 |
| מועמד (זוכה הסריקה) | 265.8 | 246.5 |
| ישן (round18, תצורה מוצמדת) | 386.9 | 194.1 |

מזווג מול המודל החדש: 84.9- פיקסלים על round27/T1 (המועמד טוב יותר ב־16.0% מהפריימים בלבד) ו־61.0- על round28/T1 (7.9%). על round28 הוא גרוע גם מהמודל הישן (58.4-, 18.8%). כלומר יתרון של 38 פיקסלים על TUNE התהפך לחיסרון של 61 עד 85 פיקסלים על מבחן מוחזק — התאמת־יתר ל־TUNE, בדיוק מה ש־T1 המוחזק נועד לחשוף. לא הוכנה בדיקה חיה; אין מה לבדוק חיה.

הגורם לפער של כ־190 פיקסלים: סטיית תנוחת ראש מתנוחת הכיול, לא זמן שחלף

לפי סעיף 3 בהנחיית המשתמש נבדקו הבדלי הקלט לפני כוונון נוסף.

ראשית, צורת המיפוי יציבה והמקור הוא שזז. אותו מודל round18 על שלוש הקלטות מוחזקות, לא־מסונן:

| הקלטה | היסט dx | היסט dy | שיפוע x | שיפוע y | חציון |
|---|---|---|---|---|---|
| round18/T1 | 12.5- | 120.5+ | 1.095 | 0.740 | 175.3 |
| round27/T1 | 225.2- | 278.4+ | 1.274 | 0.710 | 376.4 |
| round28/T1 | 100.0- | 37.3+ | 1.289 | 0.730 | 186.1 |

שיפוע Y כמעט קבוע (0.710–0.740) ושיפוע X זז מעט. כל הפער הוא היסט.

שנית, על עשר ההקלטות שנוקדו באותו מודל round18, המנבא החזק ביותר הוא סטיית ה־yaw מתנוחת הכיול, לא הזמן:

| מנבא | r מול שגיאה חציונית | r מול \\|היסט dx\\| | r מול \\|היסט dy\\| |
|---|---|---|---|
| דקות מאז הכיול | 0.650+ | 0.608+ | 0.492+ |
| \\|delta yaw_ratio\\| | **0.767+** | 0.559+ | **0.764+** |
| \\|delta eye_mid_y\\| | 0.498+ | 0.702+ | 0.431+ |
| \\|delta pitch_a\\| | 0.314+ | 0.185- | 0.157+ |
| \\|delta roll_deg\\| | 0.320- | 0.178- | 0.376- |
| \\|delta iod_norm\\| | 0.017+ | 0.234- | 0.080- |

שני המנבאים מבולבלים זה בזה רק במידה בינונית (r=+0.417 ביניהם), ושניהם שורדים ניכוי הדדי: r חלקי של yaw בהינתן זמן +0.718, ושל זמן בהינתן yaw +0.565.

המקרה שמכריע בין השניים הוא בדיוק הפער שנשאל עליו. round27/T1 נמדד 136.8 דקות אחרי הכיול עם סטיית yaw של 0.057+ ונתן 376.4; round28/T1 נמדד 139.4 דקות אחריו — מאוחר יותר — עם סטיית yaw של 0.010+ ונתן 186.1. הזמן מנבא ש־round28 יהיה שווה או גרוע יותר; ה־yaw מנבא את ההפך; ה־yaw צודק. אותו דפוס מסביר גם את round22, החריג שקטע את סיפור הזמן: 79.9 דקות, סטיית yaw קטנה, 158.1 פיקסלים.

הנקודה הכחולה החיה נשללה כהסבר: r=+0.150 בלבד בין נוכחות overlay לשגיאה, ההרצה הגרועה ביותר (round27, 376.4) הייתה **ללא** overlay וההרצה המאוחרת הטובה ביותר (round28, 186.1) הייתה **עם**. ההבדל התפעולי בין שתי ההקלטות אינו מסביר את הפער.

מנגנון: המודל אינו מקבל תנוחת ראש כקלט כלל — `head_names = ()`, 258 עמודות שהן ה־embedding בלבד. לכן סיבוב ראש אינו נכנס למודל כמשתנה אלא מזיז את ה־embedding עצמו, וזה בדיוק מופיע בפלט כהיסט קשיח ולא כשינוי שיפוע — מה שנמדד למעלה.

מגבלות שיש לשמור עליהן

n=10, נבדק אחד, מערכת אחת. `yaw_ratio` נגזר מאותם landmarks שמזינים את ה־embedding, ולכן "yaw מנבא שגיאה" הוא מנבא שמיש ומנגנון סביר פיזיקלית, אך אינו הוכחה סיבתית שסיבוב הראש הוא הגורם. הזמן לא נשלל; הוא ירד למקום שני. אין סתירה לרשומה הקודמת שפסלה תנוחת ראש: זו פסלה אותה כהסבר לכישלון **הסקאלה** ב־X ב־round1, שם הראש היה נעול ו־X נכשל בכל זאת — קביעה שנשארת נכונה. כאן מדובר בשאלה אחרת, סחיפת **היסט** בין הרצות.

מסקנה תפעולית: אין עדיין בסיס להוסיף כיול תקופתי לפי לוח זמנים — כיול מחדש נבדק ולא החזיר את הדיוק, והמנבא החזק יותר הוא סטייה מתנוחת הכיול ולא זמן. אין גם בסיס לוותר על המנוע. הכיוון הנגזר, שטרם בוצע: להתאים את round18/A **עם** מאפייני ראש ולנקד על אותן הקלטות מוחזקות, כדי לבדוק אם קלט תנוחה קונה עמידות — מדידה אופליינית, ללא מצלמה. לחלופין, גלאי סחיפת תנוחה שמתריע או מבקש כיול מחדש כשה־yaw חורג, במקום לוח זמנים. שתי האפשרויות דורשות החלטת משתמש לפני ביצוע.

בדיקות: לא שונה קוד מוצר בסבב הזה. כל הניקוד עבר דרך `gf_recal_compare.score_one` ו־`paired_difference` הקיימים. Codex QA נשאר חסום מאותה סיבה (מכסת שימוש, מתחדשת ב־14:01); גם שער העצירה האוטומטי נכשל שוב באותה שגיאה.

השער הושבת בהוראה מפורשת של המשתמש, 8.9.2026 בשעה 12:08 לערך: שער העצירה חסם שלוש פעמים ברציפות על אותה שגיאת מכסה, והמשתמש בחר במפורש להשבית אותו זמנית דרך `/codex:setup --disable-review-gate`. ההשבתה היא החלטת משתמש ואינה משנה את מצב ה-QA: הסקירה עדיין רשומה כחסומה, ואף פריט אינו מסומן DONE. יש להחזיר את השער עם `/codex:setup --enable-review-gate` ולהריץ את הסקירה אחרי 14:01.

14. ניסוי פיצוי על תנועות ראש — תוצאה שלילית מדידה — 8.9.2026

לבקשת המשתמש: ניסוי מימוש אחד וממוקד לשאלה האם שילוב נתוני הראש מייצב את המבט כשהתנוחה משתנה מעט. הניסוי רץ מקצה לקצה על נתונים קיימים, ללא הקלטה חדשה, והגיע להשוואת דיוק. התוצאה שלילית.

שלב 1 — האם נתוני האימון מתאימים? לא, משתי סיבות בלתי תלויות

טווח התנוחה בכיול צר מהסחיפה שצריך לפצות עליה. ב־round18/A טווח ה־yaw_ratio המלא על 405 שורות מתקבלות הוא 0.041- עד 0.019+, כלומר 0.060 ברוחב. הסחיפה שנמדדה בפועל בין הרצות מגיעה ל־0.061 (round26) ו־0.057 (round27) — רחבה כמו **כל** הטווח שהמודל ראה אי פעם. ב־eye_mid_x הפער חמור יותר: טווח כיול 0.008 מול סחיפה של עד 0.056, פי שבעה.

תנוחת הראש מתואמת עם המטרה בתוך הכיול. ב־round18/A: pitch_a מול target_y r=0.871+, eye_mid_y מול target_y r=0.812+, yaw_ratio מול target_y r=0.783-, roll_deg מול target_x r=0.679+. כלומר בזמן הכיול הראש זז **יחד** עם המטרה. מודל שמקבל את מספרי הראש בתנאים כאלה יכול ללמוד אותם כקיצור דרך לניחוש המטרה ולא כאיבר פיצוי — וזה נשבר בדיוק במקרה שאותו רוצים לתקן, כשהראש זז בלי שהמבט זז איתו.

שלב 2 — נבנתה גרסה ניסיונית בכל זאת

`gf_fit.py phase0 --head head3|head6` כבר קיים; לא נדרש קוד חדש. round18 הותאם מחדש עם head6 (שישה מאפייני ראש) לצד גרסת ה־head3, ונשמר בנפרד תחת `recordings/round18/models_head6/`. גיבוי של מודלי הבסיס נשמר תחת `recordings/round18/models_headnone/`.

שלב 3 — השוואה על עשר הקלטות שלא שימשו לאימון

התצורה המוצמדת בשני הצדדים, אותו מסנן, אותן שורות:

| הרצה | \\|d yaw\\| | ללא ראש (לא־מסונן) | head6 | ללא ראש (מסונן) | head6 | מזווג מסונן | טוב יותר |
|---|---|---|---|---|---|---|---|
| round18 | 0.020 | 175.3 | 185.3 | 167.3 | 176.5 | 13.2- | 23.1% |
| round19 | 0.032 | 171.7 | 182.4 | 152.4 | 155.7 | 9.7+ | 63.3% |
| round20 | 0.001 | 151.9 | 157.8 | 134.6 | 138.2 | 11.7- | 33.9% |
| round21 | 0.019 | 262.6 | 234.2 | 242.3 | 215.1 | 33.5+ | 89.4% |
| round22 | 0.029 | 158.1 | 152.4 | 173.5 | 148.0 | 31.8+ | 66.5% |
| round24 | 0.024 | 269.4 | 304.4 | 233.7 | 266.9 | 32.4- | 14.7% |
| round25 | 0.019 | 240.6 | 276.4 | 233.9 | 269.3 | 42.5- | 18.8% |
| round26 | 0.061 | 331.5 | 345.8 | 314.8 | 368.9 | 15.5+ | 62.7% |
| round27 | 0.057 | 376.4 | 339.9 | 386.9 | 343.1 | 32.9+ | 100.0% |
| round28 | 0.010 | 186.1 | 182.5 | 194.1 | 170.8 | 12.0+ | 64.0% |

שלוש התוצאות שנדרשו, בנפרד:

האם השגיאה קטנה כשהראש משנה תנוחה? **לא.** הרגישות לסחיפת ה־yaw לא ירדה כמעט בכלל: r=+0.789 ללא ראש מול r=+0.784 עם head6. בארבע ההרצות עם סחיפה גדולה (\\|d yaw\\|>0.025) הממוצע ירד מ־256.9 ל־253.9 פיקסלים — שלושה פיקסלים, בגדר רעש.

האם הביצועים הטובים נשמרו כשהראש יציב? **לא לגמרי.** בשש ההרצות עם ראש יציב הממוצע **עלה** מ־201.0 ל־206.1 פיקסלים.

יעד 120 הפיקסלים, בנפרד: **0 מתוך 10 בשתי הגרסאות.**

ההפרשים המזווגים מתנדנדים משני הכיוונים בין 42.5- ל־33.5+ פיקסלים, ואחוז הפריימים שבהם head6 טוב יותר נע בין 14.7% ל־100% לפי ההרצה. אין כיוון עקבי. זו התוצאה שחזה שלב 1.

מה הניסוי שלל ומה חסר

נשלל: הוספת מאפייני ראש **לנתוני הכיול הקיימים** אינה קונה עמידות לסחיפת תנוחה, ואינה משפרת את המצב היציב. אין טעם לבדיקה חיה של הגרסה הזו, ואין טעם לחזור על אותו כיול עם ראש קפוא.

לא נשלל: הרעיון עצמו. הניסוי בדק מאפייני ראש שנלמדו מנתונים שבהם הראש כמעט לא זז והתנוחה מתואמת עם המטרה. זהו כשל של הנתונים, לא בהכרח של הגישה.

מה שחסר, במספרים: הקלטת אימון שבה (א) טווח התנוחה רחב לפחות כמו הסחיפה הנצפית — \\|d yaw\\| של 0.061 ו־\\|d eye_mid_x\\| של 0.056 — ו־(ב) התנוחה **אינה** מתואמת עם המטרה. שתי הדרישות מושגות באותו מבנה: אותו סט מטרות בדיוק, חוזר בכמה תנוחות ראש נוחות כבלוקים נפרדים. בתוך בלוק התנוחה כמעט קבועה והמטרות משתנות; בין בלוקים התנוחה משתנה וסט המטרות זהה. כך המתאם בין תנוחה למטרה נשבר במבנה ולא בתקווה.

הטווח הזה בר־השגה בנוחות: פרוטוקול T2 הקיים, שבו הראש זז במכוון, הפיק טווחי yaw של 0.214 עד 0.409 ברוחב (round0, round1, round2) — פי שלושה עד פי שבעה מטווח הכיול הנוכחי (0.060–0.075) ורחב בהרבה מהסחיפה שצריך לכסות. כלומר אין צורך בתנוחות קיצוניות או לא נוחות.

נדרשת גם הקלטת מבחן בתנוחה שלא שימשה לאימון, אחרת ההשוואה תמדוד אינטרפולציה בין בלוקים ולא הכללה.

תיקון קוד שנדרש בדרך

`gf_filter_benchmark.support_activation` בנתה את מטריצת הקלט עם `S.assemble(rec.features[index], None, (), ())` — אפס עמודות ראש בקוד קשיח. לכן היא נכשלה עם `ValueError: schema expects 264 columns, caller has 258` על כל מודל מודע־ראש. הפונקציה שתפקידה לדווח על כיסוי המודל הייתה שמישה רק למודלים ללא ראש, והקורא לא יכול היה לראות זאת מבחוץ. עכשיו היא מרכיבה את העמודות לפי `model.schema.head_names`, כמו `predict`. נוספה בדיקת רגרסיה שמאמתת שמודל עם שני מאפייני ראש מקבל שש עמודות ולא ארבע.

תקלה תפעולית שתוקנה באותו סבב: `run_phase0` שומר תמיד אל `<recording>/models/`, ולכן ההתאמה עם head6 **דרסה** את מודלי הבסיס של round18 — אותו בסיס שכל מדידות היום נשענות עליו. הבסיס שוחזר בהתאמה חוזרת עם `--head none` ואומת מספרית: round18/T1 175.3, round27/T1 376.4, round28/T1 186.1 — התאמה מדויקת לערכים שדווחו לפני הדריסה, כלומר ההתאמה דטרמיניסטית והשחזור נאמן. מאז נשמר גיבוי מפורש ב־`models_headnone/`, ומודלי ה־head6 יושבים ב־`models_head6/`. אזהרה להמשך: אין להריץ `phase0` על תיקיית הקלטה שהמודל שלה נמצא בשימוש בלי לגבות תחילה.

בדיקות: 412 עברו; שתי הנכשלות ב־`test_m0.py` הן חוסר `screeninfo` ב־venv של הפרויקט, עוברות בסביבת ההערכה. ruff check ו־format עברו. לא שונה קוד מוצר; השינויים הם בכלי המדידה בלבד.

Codex QA: השער הושבת בהוראת המשתמש והמכסה עדיין לא התחדשה (מתחדשת ב־14:01). הסקירה נשארת רשומה כחסומה גם לסבב הזה.

15. חזרה לבסיס השמור: פרופיל, תצוגה חיה רציפה, והגנה מפני דריסת מודלים — 8.9.2026

החלטת המשתמש אחרי סעיף 14: להקפיא את ניסויי הראש, להפסיק לייצר השערות, ולהחזיר את המודל הידוע-כטוב לשימוש יומיומי. נבנה בכלי הניסוי (`gazefollower/`) ולא באפליקציה, כי שם נמצא הדיוק היחיד שנמדד אי פעם; האפליקציה הראשית מריצה מנוע אחר (MediaPipe + `RegressionModel` משלה) ואינה יודעת לטעון מודל מהמעבדה כלל.

הגנה מפני דריסת מודלים — בוצעה ראשונה, לפי דרישת המשתמש

`gf_fit.py phase0` כתב תמיד אל `<recording>/models/<name>` ודרס בלי לשאול. כך אבד הבסיס של round18 בסעיף 14. עכשיו `refuse_to_overwrite_models` עוצרת **לפני** שמתחיל האימון, כדי שסירוב לא יבזבז סריקה שלמה ולא יספיק לכתוב את המודלים הראשונים:

```
REFUSED: recordings\round18\models already holds fitted models: central-band-ridge,
central-band-svr, lib-default, ... (44 total). ... Pass --overwrite-models to replace
them on purpose, or --no-save-models to fit and score without writing any model.
```

קוד יציאה 2, בלי traceback. שבע בדיקות, ובהן אחת שמוכיחה שהשומר פועל לפני טעינת ההקלטה (`FileExistsError` ולא `FileNotFoundError`) ואחת שמוודאת ש-`--no-save-models` עדיין עובר — אחרת השומר היה דוחף אנשים אל `--overwrite`.

פרופיל שמור

`gf_profile.py` חדש: JSON תחת `profiles/`, ובו נתיב המודל, המסנן, גיאומטריית המסך, הציונים שנמדדו, ו**תנוחת הראש בזמן הכיול**. התנוחה אינה קישוט: השגיאה עוקבת אחרי סטייה מתנוחת הכיול חזק יותר מאשר אחרי זמן שחלף, ובלי לדעת היכן כוילה אי אפשר לחשב את זה. רוחב וגובה הפיקסלים אינם נשמרים במסנן אלא נגזרים מה-rig, כדי שפרופיל לא יוכל לתאר מסנן שמתאים למסך אחר. שמירה על פרופיל קיים נדחית כברירת מחדל. 14 בדיקות.

נוצר `profiles/baseline.json` ומסומן פעיל: מודל round18 המוצמד, מסנן one-euro fc=0.4/beta=0, וציונים 136.9 TUNE / 175.3 לא-מסונן / 167.3 מסונן.

תצוגה חיה רציפה

`gf_live.py` חדש. עד עכשיו הנקודה צוירה רק בזמן שמטרה על המסך, ולכן צפייה בה עלתה פרוטוקול והפסיקה כשהמטרות נגמרו — כלי מדידה, לא דרך להשתמש במערכת. עכשיו: `gf_live.py` בלי דגלים בכלל, הכול מגיע מהפרופיל. לא נכתב דבר — לא הקלטה, לא embeddings, לא מספר סבב.

מסרב לרוץ מול מסך שהפרופיל לא כויל אליו: התחזית ממופה דרך גיאומטריית המסך, ולכן מסך אחר אינו מדרדר בהדרגה אלא מייצר נקודה שגויה בביטחון בלי שדבר על המסך יאמר זאת. "tracking lost" מוצג כבאנר מפורש, כי כשאין דבר על המסך תחזית קפואה ופנים אבודות נראות זהות.

הרצה חיה ראשונה: 875 פריימים ב-30 שניות, בדיקת מודל עברה עם אקטיבציה **0.776** — מעל סף הסירוב 0.20 אך מתחת לרצועה הבריאה 0.86–0.98 שבקוד ומתחת ל-0.946–0.963 שהמודל נתן בהרצות המוקלטות של אותו בוקר. עקבי עם סחיפת תנוחה מאז הכיול ב-09:23. הערה: חציון על 40 פריימי חימום אינו בדיוק אותה מדידה כמו חציון על הקלטה מלאה.

מדידת אות סגירת העיניים — לפני בניית המחווה

לפי דרישת התוכנית, האות נמדד לפני שנבנה עליו תפריט. `gf_gesture_probe.py` חדש: שני בלוקים מונחים — 12 שניות מצמוץ טבעי, 15 שניות סגירות מכוונות של כשתי שניות — ומשווה את משכי הסגירה. פרטיות: שני floats ושני booleans לפריים בזיכרון בלבד; ה-JSON מכיל אגרגטים בלבד.

הרצה ראשונה חשפה בעיה אמיתית: **האות מפרק סגירה מתמשכת**. במצמוץ טבעי נרשמה סגירה אחת בלבד ב-360 פריימים (32ms), אך בסגירות המכוונות נרשמו 11 ריצות באורך 31–1125ms — כלומר סגירה של שתי שניות התפרקה לרסיסים, כי הערכת הפתיחות מרצדת חזרה מעל הסף באמצע האחיזה. המסקנה המיידית: הסף `recovery_confirm_ms=1500` מהתוכנית **לא היה נורה לעולם**.

התיקון: גישור על פתיחות קצרות (`bridge_ms`), עם כלל בטיחות — **אובדן פנים לעולם אינו מגושר**. גישור סולח על הערכת פתיחות מרצדת, לא על היעדרות; אחרת הסטת מבט באמצע אחיזה, או קימה מהשולחן, היו ממשיכות לצבור לעבר אישור. בדיקה נועלת את זה.

הרצה שנייה, עם סולם גישור:

| בלוק | סגירות ב-0ms | סגירות ב-100ms | מינימום | חציון | מקסימום (ב-100ms) |
|---|---|---|---|---|---|
| מצמוץ טבעי | 4 | 3 | 31 | 32 | **297** |
| סגירה מכוונת | 27 | 4 | **1172** | 2156 | 2250 |

הפרדה נקייה: 297ms מול 1172ms, פי ארבעה. 27 רסיסים התלכדו ל-4 אחיזות אמיתיות בחציון 2156ms — תואם להוראה "כשתי שניות". הערכים 100/150/200/300/400 נתנו תוצאה זהה, ולכן 100ms הוא הקטן ביותר שעובד **עם מרווח**, ולא התאמה על סף.

התפלגות שטח העין (px², מינימום בין שתי העיניים) מאשרת את המנגנון: בבלוק הטבעי p5=11, p25=103, חציון 119; בבלוק המכוון p5=1, p25=5, חציון 14. הסף 10 יושב באזור דליל.

ספים מדודים, שמחליפים את הניחושים: `bridge_ms=100`; מצמוץ טבעי גרוע ביותר 297ms (הניחוש 400 קרוב אך צר); ואישור — **1500ms אינו שמיש** שכן האחיזה הקצרה ביותר הייתה 1172ms; ערך סביר הוא כ-800ms, פי 2.7 מעל המצמוץ הגרוע ופי 1.5 מתחת לאחיזה הקצרה.

מחיר הגישור נראה בנתונים ונרשם: הוא העלה את המקסימום הטבעי מ-203 ל-297ms בכך שאיחד שני מצמוצים. זה בדיוק הסיכון שהגישור יוצר, וכאן הוא נסבל.

מגבלות: נבדק אחד, סשן אחד, כ-30 שניות, וארבע אחיזות מכוונות בלבד — בסיס דק לסטטיסטיקת "האחיזה הקצרה ביותר". שטח העין הוא px² מוחלט ולכן זז עם מרחק הישיבה; p5=11 מול סף 10 הוא מרווח דק, וישיבה רחוקה יותר תדחוף יותר מצמוצים טבעיים מעבר לסף. יש למדוד מחדש אם תנוחת הישיבה משתנה מהותית.

תיקונים נוספים בסבב

`gf_filter_benchmark.support_activation` בנתה את מטריצת הקלט עם אפס עמודות ראש בקוד קשיח ולכן נכשלה על כל מודל מודע-ראש; תוקן לפי `model.schema.head_names`, עם בדיקת רגרסיה. `predict_one`/`predict_overlay_point` הוצאו לרמת המודול ב-`gf_record.py` כדי שהתצוגה החיה והמקליט ירוצו על אותו קוד חיזוי — שני עותקים היו דרך לכך שהנקודה על המסך תסתור את המספרים שמנוקדים אחר כך. `build_gaze_follower` משותף לתצוגה ולמדידה מאותה סיבה.

בגרסה ראשונה של התצוגה, בדיקת המודל ללא פריימים הדפיסה **כלום** — שתיקה שנקראת בדיוק כמו הצלחה. תוקן; והמקרה "אין פריימים" מדווח בנפרד מ"אין קרנל", אחרי שהעברת `None` ל-`preflight_verdict` האשימה את המודל בכך שאינו SVR בזמן שהמצלמה פשוט לא ראתה פנים.

קבצים: `gf_profile.py`, `gf_live.py`, `gf_gesture_probe.py` (חדשים), `tests/test_profile.py`, `tests/test_live.py`, `tests/test_gesture_probe.py` (חדשים), ושינויים ב-`gf_fit.py`, `gf_record.py`, `gf_filter_benchmark.py`, `tests/test_fit.py`. נוצרו `profiles/` ו-`results/gesture/`.

בדיקות: 459 עוברות בסוויטת gazefollower (שתי הנכשלות הן `screeninfo` חסר ב-venv של הפרויקט, עוברות בסביבת ההערכה), 632 עוברות בסוויטת המוצר. ruff check ו-format נקיים בכל הקבצים החדשים; ב-`gf_fit.py` מספר הממצאים חזר במדויק ל-87 של HEAD, כלומר אפס תוספות.

מה טרם נבנה: מכונת המצבים של המחווה עצמה, רצף הכיול ללא ידיים מקצה לקצה, וההשוואה המזווגת בין מודל חדש לישן על אותה הקלטת בדיקה. Codex QA נותר חסום; השער הושבת בהוראת המשתמש והמכסה מתחדשת ב-14:01.

16. רצף כיול מחדש בלי ידיים, ובאג מטרות שהמשתמש זיהה מהמסך — 8.9.2026

`gf_recalibrate.py` חדש: מקליט כיול (A), מאמן, מקליט בדיקה מוחזקת (T1), מנקד את **שני** המודלים על אותם פריימים, ורק אז מחליט. `gf_record.run_session` קיבל `advance_timeout_s` כדי שפרוטוקול יתחיל מעצמו — בלי זה אין רצף בלי ידיים; Esc עדיין מבטל.

שלוש החלטות תכנון: אין חיפוש תצורה (התצורה של הפרופיל מקובעת ומאומנת מחדש, אחרת גם המודל וגם הכיול משתנים יחד ואי אפשר לזקוף את ההבדל — וזה גם מייתר את TUNE לגמרי); ההשוואה מזווגת על אותה הקלטה חדשה; והפרופיל הקודם נשאר שמור כך שחזרה אליו היא בחירה ולא שחזור.

אימות אופליין לפני שהמשתמש נגע: `fit_pinned` משחזר את המודל ש-`phase0` מייצר **בדיוק** (191.8 מול 191.8 על round28/T1), וההשוואה משחזרת את המספרים שנמדדו קודם.

באג שנתפס בבדיקה לפני שהגיע למשתמש: כלל האימוץ הראשון היה `delta > 0`, וקרא ל-+5.9px על 54% מהפריימים "יותר טוב" — כלומר היה מחליף פרופיל על רעש. עכשיו נדרשים **לפחות 10px ולפחות 60% מהפריימים**. שני המספרים הם שיקול דעת מוצהר ולא מדידה; הרציונל: החלפת כיול שעובד מוותרת על כמות ידועה תמורת אחת שנמדדה על ארבעים שניות.

הבאג שהמשתמש זיהה — מטרות מחוץ לתחום

המשתמש הבחין מהמסך שמטרות ה-T1 יצאו מהתחום 0.3–0.7 בעוד הנקודה הכחולה נראתה טובה מאוד בתוך התחום. הוא צדק. `gf_record.main` מחליף את קובץ המטרות ל-`targets_central.json` כשנתון `--x-range`, אבל `gf_recalibrate` קרא ל-`run_session` ישירות וקיבע את `targets.json` — ועקף את ההחלפה. `targets.json` משתרע על x=0.147..0.876, כלומר רק 4 מתוך 10 מטרות בתוך התחום.

המחיר, בניקוד חוזר של round4 לפי מיקום המטרה (מסונן):

| מודל | כל 10 | בתוך התחום | מחוץ לתחום |
|---|---|---|---|
| ישן (round18) | 288.4 | 233.6 | **963.8** |
| חדש (round4) | 230.2 | 160.9 | **773.2** |

מחוץ לתחום השגיאה היא 773–964 פיקסלים, והחציון על "כל 10" תיאר בעיקר מטרות שהמודל מעולם לא כויל אליהן. אומת שכל ההרצות הקודמות (round18–round28) השתמשו בקובץ המרכזי ולכן **אינן** נגועות — הפגם היה בקוד החדש בלבד. התיקון: `band_target_files` ב-`gf_record.py`, משותפת ל-main ולכל קורא תוכניתי, בדיוק כי כך הוחמץ — ההחלפה חיה רק ב-CLI.

התוצאה הנקייה, בתוך התחום

round30, כיול טרי מול הבסיס, אותם 460 פריימים, אותו מסנן: **הישן 188.9px, החדש 205.8px, הפרש מזווג −12.5px, החדש טוב יותר ב-45% מהפריימים בלבד.** הכלל סירב להחליף ו-`baseline` נשאר פעיל. זהו הכיול הטרי השלישי היום שאינו מנצח את round18 (round27, round30), ומחזק את המסקנה מסעיף 13.

סתירה שנרשמת ולא מיושבת: round4 אמר שהחדש טוב ב-73px בתוך התחום, round30 אמר שהוא גרוע ב-12.5px, בהפרש של כחצי שעה. בתוך כל הרצה הזיווג תקף, אך "בתוך התחום" ב-round4 הוא ארבע מטרות מקובץ אחר ובround30 עשר מטרות מרכזיות — סטים שונים, ולכן שתי ההרצות אינן בנות-השוואה זו לזו. אי-היציבות בין הרצות שנצפתה כל היום נמשכת.

הבסיס עצמו נשחק: 167.3 על ה-T1 שלו בבוקר, 188.9 עכשיו — ועדיין מנצח כל כיול טרי.

שומרים שנוספו אחרי כישלון אמיתי

הרצה אחת נכשלה כי המצלמה לא החזירה פריימים (`Failed to grab frame`, watchdog אחרי 5 שניות, אפס שורות). הקוד המשיך בכל זאת לאימון וקרס עם `need at least 4 training rows` — הודעה על המודל עבור תקלה של המצלמה. השומר בדק `aborted` אך לא `watchdog_tripped`, שנשאר False בטריפ. עכשיו נבדקים שניהם, נדרשת נוכחות A ו-T1, ונוספה `require_usable_calibration` שמסרבת לפני האימון ומדווחת כמה שורות ומכמה מטרות נאספו בפועל. אומת על נתונים אמיתיים: round30 עובר עם 405 שורות, round29 הכושל נדחה בהודעה שמצביעה על הסיבה הנכונה.

תוקן גם מספור הסבבים: הגרסה הראשונה בחרה את המספר הפנוי הנמוך ביותר ולכן ההקלטה נחתה על **round4**, בין ניסויים מיום אחר. עכשיו ממשיכים אחרי הגבוה ביותר.

קבצים: `gf_recalibrate.py`, `tests/test_recalibrate.py` (חדשים), שינויים ב-`gf_record.py`. נוצרו `recordings/round4`, `round29` (כושל), `round30`, ופרופיל `baseline_20260908_102502` שנוצר בהרצה הנגועה ואינו פעיל.

בדיקות: 487 בסוויטת gazefollower (שתי הנכשלות הן `screeninfo` חסר ב-venv), 632 בסוויטת המוצר. ruff נקי. Codex QA נותר חסום.

17. M3-03 — בחירה בשהיית מבט (Dwell): המנגנון עובד, המשימה נכשלת — 8.9.2026

נבנה מסך תרגול לבחירת כפתורים בשהיית מבט, על המודל הקפוא (`baseline` = round18). קבצים חדשים: `gf_dwell.py` (מכונת מצבים טהורה), `gf_dwell_practice.py` (פרוטוקול ודוח), `Display.draw_practice`/`_draw_progress_ring`, ובדיקות `test_dwell.py` (27) ו-`test_dwell_practice.py` (33).

הערה למקורות האמת: `spec.MD:203-245` ממקם Dwell Click **שלישי** ב-M3, אחרי סמן ומנוע מחוות; כאן הוא נעשה ראשון. זה חורג מהסדר במכוון ואינו חוצה את החסימה של M3-00 ("אין להזיז סמן אמיתי על סרגל לא מאומת") משום שאין כאן קלט OS כלל. בנוסף `TECHNICAL_SPEC.md:487-501` מסמן `Dwell → Alternative selection mode` כ**כיוון מוצע התלוי בהחלטות D-04/D-05** — הניסוי הזה מודד היתכנות ואינו מכריע את המיפוי.

הפריסה נגזרה ממדידה, לא מהחציון

השגיאה נשלטת על ידי **הטיה** ולא פיזור: פיזור בתוך קיבוע P95 של 58–77px מול הטיה של עד 446px. Dwell ממצע על פני החלון שלו, ולכן מסלק פיזור ואינו מסלק הטיה. כשברים: |dx| 0.051 מרוחב המסך אך |dy| 0.387 מגובהו — ולכן הכפתורים בגובה מלא, לא רשת.

תיקון לניתוח שלי עצמו, שנתפס בבדיקה: שכן שגוי דורש **חצי-רוחב ועוד המרווח**, לא המרווח לבדו. בשתי הפריסות השכן מחוץ להישג יד של ההטיה הנמדדת; הכשל הריאלי הוא פספוס.

שלוש הרצות, וכשלים שהיו שלי

**הרצה 1 — 9/20.** 8 מתוך 11 ההפעלות השגויות נחתו על מטרת הניסיון **הקודם**, ו-7 נורו ברגע המוקדם ביותר האפשרי. הסיבה: `engine.reset()` בתחילת כל ניסיון ניטרל את הנעילה, וכפתור שהמפעיל עדיין נח עליו ירה לפני שהספיק לזוז. תוקן: הנעילה נשמרת בין ניסיונות, ונוסף שער ניטרלי — ניסיון נמדד רק אחרי שהמבט נראה מחוץ לשני הכפתורים. ניסיון שלא הגיע לניטרלי **אינו נמדד כלל** (`scored=False`) ומדווח בנפרד; להריץ אותו בכל זאת היה מחזיר בדיוק את הזיהום שהשער נועד להסיר.

**הרצה 2 — 9/20 עם פרוטוקול נקי** (אפס דילוגים, אפס הפעלות לפני התחלה, אפס אובדן מעקב). כלומר ה-carry-over לא היה ההסבר המלא.

**הרצה 3 — 8/20** אחרי שההנחיה הועברה מהפינה השמאלית העליונה למרכז האזור המת. ההשערה שקריאת ההנחיה מושכת את המבט שמאלה — **הופרכה**.

תיקון נוסף לניסוח שלי: "מרחק ממרכז הכפתור" אינו מדד הטיה — מותר להסתכל בכל מקום בתוך הכפתור. השדה שונה ל-`gaze_offset_from_centre` עם הסתייגות מפורשת בדוח. באותו אופן נמחק חישוב "gain 0.18" שהתבסס על הנחה שהמפעיל הסתכל במרכזים.

מה שכן נקבע במדידה

**המודל תקין.** round31 הקליט עשר מטרות בדיוק בשני מרכזי הכפתורים, עם סימון מפורש להסתכל עליו: נתבקש x=0.38 → חזה 0.362; נתבקש x=0.62 → חזה 0.628. **הפרדה 0.267 עבור 0.240 מבוקשים, gain 1.11**, וחמש מטרות ה-RIGHT בטווח של 0.021 בלבד. Y gain 1.02.

**מנגנון ה-Dwell תקין.** תיעוד מסלול ב-20Hz על שמונה ניסיונות מראה שבכל אחד הנקודה יצאה מהאזור המת, נסעה חלק לכפתור אחד ונשארה שם עד ההפעלה: אפס קפיצות, אפס בריחות, אפס אי-בחירות, אפס הפעלות חוזרות. ההשערה שהנקודה מגיעה ובורחת ומאפסת את המונה — הופרכה גם היא.

מה שלא נפתר

בכל שלוש ההרצות הכשל זהה: **הנקודה נוסעת בביטחון לכפתור ההפוך ונשארת בו.** לא על גבול, לא בהיסוס — x בין 0.311 ל-0.400 כשנתבקש RIGHT, ובין 0.558 ל-0.631 כשנתבקש LEFT. המפעיל מדווח שהסתכל לפי ההוראות ושוויזואלית הנקודה נראתה תקינה.

שלושה דברים אומתו בנפרד — המודל, המנגנון, והפרוטוקול — ובכל זאת המשימה נכשלת. הסתירה הזו לא יושבה, ואין לי הסבר מבוסס. 8–9 מתוך 20 בשתי הרצות מלאות הוא **סביב מקריות** במשימה של שתי אפשרויות.

**הפעלות לא מכוונות בזמן מנוחה מחמירות: 15 → 19 → 24 ב-60 שניות.** האזור המת של 0.08 צר מכדי לנוח בו. זו בעיה פתוחה שלא טופלה, והיא נפרדת ממעבר הניסיונות. "False activation" רשום ב-`TECHNICAL_SPEC.md:643-657` כיעד שטרם נמדד — עכשיו יש לו מספר, והוא גרוע.

מסקנה: **בחירה בשהיית מבט אינה שמישה כרגע**, ואין להתקדם ממנה לסמן או לקליקים. יעד 120px נשאר לא מושג ומדווח בנפרד.

בדיקות: 547 עוברות (שתי הנכשלות הן `screeninfo` חסר ב-venv), ruff נקי בקבצים החדשים. אפס קלט OS בכל המסלולים. הפרופיל, המודל וההקלטות לא שונו.

תיקון לסעיף 17 — הבחירה עובדת. המסקנה הקודמת הייתה שגויה — 8.9.2026

**סעיף 17 קבע ש"בחירה בשהיית מבט אינה שמישה כרגע". זה לא נכון, וההרצה הבאה הפריכה זאת: 20 מתוך 20.**

```
נתבקש : L R L R L R L R L R L R L R L R L R L R
נבחר  : L R L R L R L R L R L R L R L R L R L R
```

אפס טעויות, אפס פספוסים, אפס אי-בחירות, אפס הפעלות כפולות, אפס דילוגי שער. חציון זמן לבחירה 1204ms, P90 2564ms. `results/dwell/a_20260908_123724.json`.

מה באמת נכשל בשלוש ההרצות הראשונות

לא המודל, לא ה-Dwell, לא החיבור ולא הדיווח — כולם היו תקינים לכל אורך הדרך, כפי שהמעקב מקצה לקצה הראה. מה שנכשל היה **המדידה עצמה**.

המפעיל בחר להחליף צד בעקביות — שמאל, ימין, שמאל, ימין. סדר המטרות שבניתי ב-`trial_order` מחלק את הניסיונות לזוגות שכל אחד מכיל את שתי המטרות, ולכן הוא **מתחלף צד ב-79% מהמעברים**. מפעיל שמחליף תמיד ומפעיל שעוקב מושלם אחרי ההנחיה מייצרים כמעט אותה סטטיסטיקה, ולכן שום ניתוח של התוצאות לא יכול היה להפריד ביניהם. מול סדר כזה, החלפה עקבית נותנת בערך 50% — וזה בדיוק מה שהתקבל, 8 ו-9 מתוך 20.

התיקון: `--pattern alternate` מייצר החלפה מוחלטת, ואז **התבנית עצמה היא התשובה הצפויה** וכל סטייה ממנה היא שגיאת מערכת ולא אי-התאמה לסדר שהמפעיל מעולם לא עקב אחריו.

כשלים בשיפוט, לתיעוד

בניתי מדידה שאינה מסוגלת להבחין בין "המערכת שגתה" לבין "המפעיל עשה משהו אחר", ואז ביליתי שלוש הרצות בחיפוש באג בצד הלא נכון. הצעתי שלוש השערות — carry-over, מיקום ההנחיה, ובריחת הנקודה — שתיים מהן הופרכו במדידה ואחת תוקנה בצדק. על סמך זה הצעתי **לוותר על הציר האופקי או לעצור את הפרויקט**; המשתמש עצר את הקפיצה הזו ודרש הסבר מקצה לקצה של כישלון בודד, שהוא שהוביל בסופו של דבר לשאלה שחשפה את הסיבה. ההצעה לוותר לא הייתה מבוססת על הראיות שהיו בידי.

מה שנשאר פתוח, ומה שחוסם התקדמות לקלט OS

**הפעלות לא מכוונות בזמן מנוחה: 15 → 19 → 24 ב-60 שניות.** ההרצה שעברה 20/20 רצה עם `--idle-seconds 0` ולכן אין לה מספר חדש — הבעיה לא נמדדה מחדש ולא טופלה. בקצב של 24 לדקה מדובר בהפעלה מיותרת כל שתי שניות וחצי.

לפי CLAUDE.md 4.1 ו-`TECHNICAL_SPEC.md:503-519`, מערכת שבוחרת נכון כשמתכוונים אבל בוחרת גם כשלא — אינה מוכנה להזיז סמן או לייצר קליקים. **זה החסם לפני M3-01/M3-02**, בנוסף לחסם הקיים של M3-00 ("אין להזיז סמן אמיתי על סרגל לא מאומת").

הישג M3-03 נכון לעכשיו: מנגנון שהייה שנבדק ועובד, עם ספים מדודים, בתוך מסך תרגול בלבד, ללא קלט OS. יעד 120px נותר לא מושג ומדווח בנפרד. Dwell כמנגנון בחירה נותר "מוצע" לפי `TECHNICAL_SPEC.md:487-501` עד D-04/D-05.

18. M3-00 נסגר, ו-M3-02 — סמן Windows אמיתי לפי מבט, בלי קליקים — 8.9.2026

M3-00 — אימות הסרגל

`gf_screen_check.py` חדש, קריאה בלבד: אינו מזיז סמן, אינו מייצר קלט, אינו כותב דבר. בודק כל תנאי שחייב להתקיים לפני שחלק ממסך הופך לפיקסל, ומדווח כל אחד בנפרד — "הסמן במקום הלא נכון" הוא אותו סימפטום לשלושתם.

ההרצה הראשונה נכשלה בשניים, ושניהם היו אמיתיים:

**התהליך לא היה DPI-aware.** בלי הצהרה, Windows מוסר לתהליך קואורדינטות מווירטואלות: על מסך ב-125% הוא מדווח 4096x1152 עבור פאנל 5120x1440, וסמן שמכוון לקצה הימני נוחת חמישית מסך קצר — בעקביות, בלי שגיאה בשום מקום. התיקון: `ensure_per_monitor_dpi_aware()` מצהיר PER_MONITOR_AWARE_V2 **לפני** שנקרא מדד כלשהו או נפתח חלון.

**הפרופיל לא שמר זהות מסך, רק גודל.** שני מסכים יכולים לחלוק רזולוציה — בדיוק הכשל ש-M3-00 מצביע עליו. ההקלטה כן החזיקה את המידע: `screen_id: LS49C95xU` ומידות פיזיות 1192x336 מ"מ. נוסף שדה `screen` ל-`Profile` עם `screen_identity()`, והפרופיל `baseline` עודכן בו בלי לגעת במודל, במסנן או ב-rig. הבדיקה בזמן ריצה משווה מילימטרים, כי מחרוזת הדגם אינה חשופה לספריית התצוגה אך המידות כן, והן נבדלות בין פאנלים שחולקים רזולוציה.

אחרי שני התיקונים: **שבע מתוך שבע עוברות. VERIFIED.** M3-00 אינו חוסם עוד את M3-02.

M3-02 — מתאם הסמן

`gf_cursor.py` חדש. זהו הקובץ היחיד בספרייה הזו שנוגע במערכת ההפעלה, והוא נכתב כדי שיהיה קל לביקורת: פונקציה אחת קוראת ל-Windows, היא מזיזה מצביע ותו לא.

**אין קליקים, ואין מסלול קוד שיכול ללחוץ.** בדיקה קוראת את קוד המקור ומוודאת שאין בו `mouse_event`, `SendInput`, `MOUSEEVENTF` — כדי שקליק לא ייכנס בשקט בהמשך. הזזת מצביע ניתנת לביטול על ידי הזזה חזרה; קליק לא, ובחירה במנוחה עדיין ייצרה הפעלה אחת לדקה.

שערי הבטיחות, כולם מהיום הראשון ולא "אחר כך":

- **כבוי כברירת מחדל.** `--move-cursor` לבדו מסורב; נדרש גם `--i-mean-it`. שני שערים נפרדים בכוונה: האחד הוא המפעיל אומר שהתכוון, השני הוא המכונה מסכימה שהסרגל אמין.
- **סרגל מאומת.** מסרב לרוץ אם `check_profile_screen` נכשל.
- **הקפאה על אובדן.** אין נקודה, נקודה ישנה, מצמוץ, פנים אבודות → הסמן עוצר במקומו. לעולם אינו מוזז לניחוש. הקפאות נספרות, כדי ש"הסמן לא זז" יובחן מ"לא ביקשו ממנו דבר".
- **הגבלת קפיצה** של 400px לפריים, שנשארת בתוקף גם אחרי הקפאה.
- **השהיה ללא ידיים** דרך תפריט המחוות, כשהנקודה ממשיכה להיות מוצגת.
- **שחרור ביציאה** — הסמן חוזר למקום שנמצא בו, גם כשהסשן נופל על חריגה, והשחרור עצמו לעולם אינו מחליף את השגיאה המקורית בשגיאה משלו.

`enabled=False` הוא סימולציה מלאה: מחשבת ורושמת כל מיקום ואינה קוראת לדבר. שני המסלולים נבדלים בשורה אחת בדיוק, כך שמה שנבדק בסימולציה הוא מה שרץ באמת.

אימות חי ותיקון תחושה

המפעיל הריץ: "נראה סבבה לגמרי, עקב, מעט קופצני". הקופצניות צפויה — `fc=0.4/beta=0` כוונן לייצב נקודה עומדת ולא לעקוב אחרי סמן, ו-`beta=0` אומר שאין בו רכיב אדפטיבי כלל.

**המסנן של הפרופיל לא שונה**, כי הוא חלק מהתצורה שאומתה ב-20/20 במשימת הבחירה ושינויו היה מבטל את התוצאה. במקום זה נוספה החלקה על המצביע בלבד: 35% מהמרחק ליעד לפריים ואזור מת של 12px, שמתחתיו המצביע כלל אינו זז — עין לעולם אינה עומדת לגמרי, ובלי רצפה כזו המצביע רוטט לנצח סביב מטרה שמסתכלים עליה בקביעות.

המפעיל בדק והעדיף `smoothing=0.6, dead_zone=6`. הערכים נשמרו בפרופיל בשדה `cursor`, נפרד מ-`filter` בכוונה: האחד שייך למודל, השני לתצוגה בלבד. דגל בשורת הפקודה עדיין גובר לניסוי חד-פעמי.

בדיקות: 567 עוברות (שתי הנכשלות הן `screeninfo` חסר ב-venv של הפרויקט). 20 בדיקות למתאם, **כולן עם Windows מזויף** — אף בדיקה בפרויקט אינה מזיזה סמן אמיתי. נבדקו: הסירוב ללא אישור, זהות בין הסימולציה למסלול האמיתי, הקפאה על אובדן, השהיה, הגבלת קפיצה, מחיקת היסטוריית ההחלקה בהקפאה, ושחרור המצביע כולל תחת חריגה.

מה שנשאר פתוח: קליקים אינם מחוברים ולא יחוברו עד שיהיה מנגנון מפורש להפעלת הבחירה ולהשהייתה — הפעלה לא מכוונת אחת לדקה מקובלת לתנועת מצביע, לא ללחיצה. יעד 120px נותר לא מושג ומדווח בנפרד. `--pattern alternate` הוא סדר צפוי, ולכן ה-20/20 אינו מכסה סדר משתנה או חזרה על אותו כפתור.
